[build]
  publish = "dist"

# Fallback SPA: senza questa regola, un caricamento diretto su una rotta diversa da "/"
# (es. un deep link email) restituisce 404 invece di servire index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
