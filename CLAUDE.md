# Guida allo Sviluppo: App Gestione Prenotazioni
**Stato**: Fase 1 Completata (Backend Area Piscina + Core Autenticazione) - Fase 2 in corso (Autenticazione Frontend Axios completata, UI Kit gluestack-ui + NativeWind integrata, schermate base di navigazione staff completate, mappa spaziale postazioni piscina completata, gestione staff su invito + reset password self-service completati, landing "Area Cliente" con selettore servizi completata, flusso di prenotazione piscina self-service lato cliente completato con biglietto PDF scaricabile subito dopo l'invio, notifiche in-app staff per nuove prenotazioni self-service completate)

Questa guida documenta l'architettura e la logica di business implementata per l'ecosistema Django/PostgreSQL e per il client React Native, con le configurazioni specifiche per le 4 app backend principali e per lo strato di autenticazione del frontend.

---

## 0. Stack Tecnologico
*   **Database**: PostgreSQL 17
*   **Backend**: Python 3.13, Django 6.0.4
*   **Frontend Mobile**: React Native 56.0.x (Expo SDK 56), Expo Router (routing basato su file system, `app/`)
*   **Librerie Core Backend**: Django REST Framework, djangorestframework-simplejwt (Auth, incl. `token_blacklist`), WeasyPrint (PDF), django-cors-headers, django-filter, django-anymail (invio email transazionali, backend Resend — attivazione account e reset password staff), pytest + pytest-django + factory_boy (test automatizzati, vedi sezione 9).
*   **Librerie Core Frontend**: Axios, expo-secure-store, NativeWind v5 (Tailwind CSS v4) come motore di styling, gluestack-ui v5 (alpha) come UI kit di componenti copy-paste, Zod (validazione degli schemi dei form lato client, es. creazione/modifica staff e i flussi di password), expo-file-system + expo-sharing (download e apertura/condivisione del biglietto PDF su nativo, vedi sezione 7), react-native-paper + react-native-paper-dates (solo per il `TimePickerModal` del form di prenotazione self-service, sezione 7 — l'unico punto dell'app con un secondo design system oltre a gluestack-ui, confinato al gruppo di rotte `/cliente/*`), @expo/vector-icons (icone del `TimePickerModal`).

---

## 1. Architettura delle App Django

Il progetto è suddiviso in 4 app principali: `users`, `struttura`, `prenotazioni`, e `menu`.

### App `users` (Gestione Accessi e Anagrafica)
1.  **Modello `Utente` (Staff/Admin)**
    *   **Scopo:** Gestire gli accessi al backend e alla dashboard amministrativa.
    *   **Campi Base:** `id` (UUID), `username`, `email`, `password`, `is_active` (booleano standard di Django, usato qui anche come interruttore "account disattivato" gestibile dai superuser — vedi `set_active` sotto).
    *   **Permessi:** Ogni nuovo utente creato tramite l'API ha `is_staff=True` ma **`is_superuser=False` di default**. Solo un account con `is_superuser=True` (creato via `python manage.py createsuperuser`, non tramite API) può gestire e monitorare tutti gli altri account staff/admin della piattaforma.
    *   **Creazione su invito (niente password lato admin):** `POST /api/v1/users/staff/` accetta solo `{ username, email }` (`UtenteCreateSerializer`, usato esclusivamente per l'azione `create` tramite `UtenteViewSet.get_serializer_class`). L'utente viene creato con `set_unusable_password()` — **nessuna password è utilizzabile finché lo staff non la imposta da sé** — e `UtenteViewSet.create()` invia subito un'email di attivazione (`users/utils.send_activation_email`). Le altre azioni (list/retrieve/update/delete) usano invece `UtenteSerializer`, che espone `id, username, email, is_superuser (read-only), is_active` e **non include mai la password**.
    *   **Email univoca:** `email` non ha un vincolo `unique` a livello di modello/DB (campo ereditato da `AbstractUser`, `blank=True` di default) — l'unicità è garantita solo a livello applicativo, stesso approccio già usato per `Cliente.telefono` (vedi sotto). `validate_email` (helper condiviso `_validate_email_non_duplicato` in `users/serializers.py`) blocca sia `UtenteCreateSerializer` (creazione) sia `UtenteSerializer` (update, escludendo l'istanza corrente dal controllo) se esiste già un altro account staff con la stessa email, confronto **case-insensitive** (`email__iexact`, stesso criterio già usato da `reset_password_request` per non rischiare un `MultipleObjectsReturned` quando cerca l'utente da un'email in chiaro).
    *   **Attivazione account:** `POST /api/v1/users/staff/activate/` (pubblico, `AllowAny`) riceve `{ uid, token, password }` e imposta la prima password. `uid`/`token` sono generati con lo stesso meccanismo dei token di reset password di Django (`urlsafe_base64_encode(pk)` + `django.contrib.auth.tokens.default_token_generator`, tempo-limitati e a uso singolo perché invalidati da cambi di stato dell'utente). La password è validata lato server con `django.contrib.auth.password_validation.validate_password`.
    *   **Reset password self-service:** `POST /api/v1/users/staff/reset-password/request/` (pubblico) accetta `{ email }` e — se corrisponde a un account attivo — invia un'email con link di reset (`send_password_reset_email`); risponde **sempre** con lo stesso messaggio generico, per non rivelare quali indirizzi email sono registrati. `POST /api/v1/users/staff/reset-password/confirm/` (pubblico) riceve `{ uid, token, password }` e imposta la nuova password, stesso meccanismo token dell'attivazione.
    *   **Attiva/disattiva account:** `PATCH /api/v1/users/staff/{id}/set-active/` (riservato ai superuser tramite `IsSuperUser`, come le altre azioni non elencate esplicitamente in `get_permissions`) riceve `{ is_active: bool }`. Risponde **400** se un superuser tenta di disattivare **il proprio stesso account** (eviterebbe un lockout senza altro modo per ripristinarlo se non da `manage.py createsuperuser`). Nota: questo endpoint dedicato esiste perché il `PATCH` generico su `UtenteSerializer` potrebbe in teoria modificare `is_active` senza passare da questo guardrail — il frontend usa sempre e solo `set-active` per questa operazione (vedi sezione 6).
    *   **Permessi API (`UtenteViewSet`, in `users/views.py`):** `IsSuperUser` (`users/permissions.py`) di default su tutte le azioni standard del `ModelViewSet`. `get_permissions()` apre `me` a `IsAuthenticated`, e `activate`/`reset_password_request`/`reset_password_confirm` a `AllowAny` (sono raggiunte da utenti che, per definizione, non hanno ancora una sessione).
    *   **Invio email (`users/utils.py`):** `send_activation_email`/`send_password_reset_email` compongono il link come `f"{settings.FRONTEND_ACTIVATION_URL}?uid={uid}&token={token}"` (idem per il reset) e chiamano `django.core.mail.send_mail`. `FRONTEND_ACTIVATION_URL`/`FRONTEND_RESET_URL` valgono `"legole://activate"` / `"legole://reset-password"` — `legole` è lo **scheme** registrato in `le-gole-fe/app.json`, che Expo Router risolve rispettivamente su `app/activate.tsx` e `app/reset-password.tsx` (vedi sezione 6). **Attenzione:** questi deep link `scheme://` funzionano solo in una build standalone o in un dev client — in Expo Go durante lo sviluppo il formato è diverso (`exp://<host>:8081/--/activate`), quindi il flusso email va testato su una build reale o adattando temporaneamente l'URL.
    *   **Configurazione email (`backend/settings.py`):** `EMAIL_BACKEND = "anymail.backends.resend.EmailBackend"`, `ANYMAIL = {"RESEND_API_KEY": env("RESEND_API_KEY")}` (mai hardcoded, letta da `.env`), `DEFAULT_FROM_EMAIL = "delivered@resend.dev"` — è l'indirizzo di **test** di Resend, va sostituito con un mittente sul dominio verificato prima di andare in produzione.
    *   **`DEBUG`:** ora letto da env (`env.bool('DEBUG', default=False)`) invece di essere hardcoded a `True`; il valore locale resta `True` tramite `.env`.
    *   **Autenticazione:** Sistema JWT (`djangorestframework-simplejwt`) configurato per mantenere le sessioni attive fino a **30 giorni** (access token), con refresh token della durata di **60 giorni**. `ROTATE_REFRESH_TOKENS` e `BLACKLIST_AFTER_ROTATION` sono attivi e resi effettivi dall'app `rest_framework_simplejwt.token_blacklist` (aggiunta a `INSTALLED_APPS` con le relative migrazioni applicate): ogni refresh invalida il token precedente ed emette una nuova coppia access/refresh.
2.  **Modello `Cliente` (Guest/Customer)**
    *   **Scopo:** Rappresentare l'anagrafica di chi usufruisce del servizio.
    *   **Campi:** `id` (UUID), `nome`, `telefono`, `created_at`, `updated_at`. **Nessun campo `note`:** le note sono per-prenotazione, non per-anagrafica (vedi `Prenotazione.note` sotto) — un cliente che torna con richieste diverse (es. allergie solo per quella visita) non deve trascinarsi le note di una prenotazione precedente.
    *   **Creazione pubblica e upsert per telefono:** `POST /api/v1/users/clienti/` è ora **pubblico** (`AllowAny` solo per `create`, le altre azioni di `ClienteViewSet` restano `IsAuthenticated`) — pensato per un futuro flusso di auto-registrazione lato "Area Cliente" (vedi sezione 7). `ClienteViewSet.create()` non crea sempre un nuovo record: fa un `get_or_create(telefono=...)` dentro una `transaction.atomic()` e, se il cliente esiste già, **aggiorna** `nome` sull'esistente invece di duplicarlo. Risponde `201` se creato, `200` se aggiornato un cliente già esistente. Nota: `telefono` non ha un vincolo `unique` a livello di modello/DB, quindi l'unicità è garantita solo dalla logica applicativa di questa `create()`, non dal database.

### App `struttura` (Risorse Fisiche e Inventario)
1.  **Modello `Piscina_Inventario`**
    *   **Scopo:** Definire la capacità massima, i listini prezzi, e gli orari di apertura della piscina.
    *   **Campi:** `id`, `nome`, prezzi (ingresso, ombrellone, ecc.), quantità totali delle risorse, `isActive` (per definire il listino in vigore).
    *   **Campi Temporali:** `orario_apertura`, `orario_chiusura`, `created_at`, `updated_at`.
2.  **Modello `Postazione`**
    *   **Scopo:** Rappresentare lo spot fisico (un ombrellone o un gazebo) sulla mappa spaziale di un dato `Piscina_Inventario`. È un dato **strutturale** (il layout fisico dell'area piscina), non legato a una data specifica — vedi `OccupazionePostazione` nell'app `prenotazioni` per l'assegnazione giornaliera.
    *   **Campi:** `id`, `inventario` (FK a `Piscina_Inventario`, `CASCADE`), `tipo` (`OMBRELLONE` / `GAZEBO`), `numero` (etichetta identificativa, univoca per inventario), `pos_x`/`pos_y` (float 0-100, posizione percentuale sul canvas della mappa, indipendente dallo zoom), `created_at`, `updated_at`.
    *   **API:** `PostazioneViewSet` (`struttura/views.py`) su `/api/v1/struttura/postazioni/`, filtrabile per `inventario` e `tipo`.
3.  **Modelli `Sala` e `Tavolo`** *(Da sviluppare)*
    *   **Scopo:** Mappatura della planimetria del ristorante per le prenotazioni dei tavoli.

### App `prenotazioni` (Logica Transazionale)
1.  **Modello `Prenotazione` (Base Astratta)**
    *   **Scopo:** Modello padre che accorpa i dati comuni (`abstract = True`).
    *   **Campi:** `id`, `cliente_id` (ForeignKey verso `users.Cliente`), `data`, `ora`, `stato`, `note` (`TextField` opzionale, `blank=True, default=''` — note aggiuntive libere, es. allergie o richieste particolari; vive qui e non su `Cliente` perché è specifica di *questa* prenotazione, non dell'anagrafica), `created_at`, `updated_at`.
2.  **Modello `Prenotazione_Piscina`**
    *   **Scopo:** Registrare le risorse bloccate per l'area piscina.
    *   **Campi:** `ingressi`, `ombrellone`, `gazebo`, `lettino`, `sdraia`. ForeignKey verso `struttura.Piscina_Inventario` (con vincolo `PROTECT` per preservare lo storico).
    *   **Serializer:** `PrenotazionePiscinaSerializer` espone anche `cliente_nome`/`cliente_telefono` (read-only, `source='cliente_id.nome'`/`source='cliente_id.telefono'`) per evitare una join lato frontend quando serve solo il nome/telefono del cliente (es. mappa postazioni); `note` è invece un campo reale e scrivibile del modello (ereditato da `Prenotazione`), incluso automaticamente da `fields = '__all__'`.
    *   **Eliminazione di un `Piscina_Inventario` con prenotazioni collegate:** il `PROTECT` blocca correttamente la cancellazione (per non perdere lo storico), ma senza gestione esplicita Django solleva `ProtectedError` e DRF la propaga come 500 grezzo. `PiscinaInventarioViewSet.destroy()` (`struttura/views.py`) la intercetta e risponde `400` con un messaggio leggibile; il frontend (`PiscinaInventarioSection.tsx`, `confirmDelete`) lo mostra con un `alert`/`Alert.alert` immediato — l'eliminazione dalla card non apre l'`Actionsheet`, quindi il vecchio `error` di stato (mostrato solo dentro il form) non sarebbe mai stato visibile all'utente.
    *   **Regola di eliminazione data-based:** `PiscinaInventarioViewSet.destroy()` distingue le prenotazioni per data rispetto a `timezone.localdate()` (fuso `Europe/Rome`, coerente con `USE_TZ=True`). Se esiste **almeno una** `Prenotazione_Piscina` con `data >= oggi` (di qualsiasi stato, incluse `CANCELLED`) → **400**, eliminazione bloccata. Se esistono solo prenotazioni con `data < oggi`, queste vengono **eliminate automaticamente** (in una `transaction.atomic()` insieme all'inventario) per liberare il vincolo `PROTECT`; l'anagrafica `Cliente` collegata **non** viene mai toccata (un cliente può avere altre prenotazioni altrove).
    *   **`create` pubblico (prenotazione self-service Area Cliente):** `PrenotazionePiscinaViewSet.get_permissions()` apre solo l'azione `create` ad `AllowAny` (stesso pattern di `ClienteViewSet.create`, vedi sopra) — il cliente finale che prenota da `/cliente/piscina/{id}` non ha un token JWT. Tutte le altre azioni (list/retrieve/update/delete/`scarica_biglietto`) restano `IsAuthenticated`, per non esporre pubblicamente nome/telefono/note delle prenotazioni altrui. Una prenotazione creata da questo flusso nasce **`PENDING`** (default del modello) — a differenza del walk-in creato dalla mappa piscina (sezione 5), che nasce già `CONFIRMED` perché il cliente è fisicamente presente: qui invece lo staff deve confermarla.
    *   **Azione pubblica `disponibilita`:** `GET /api/v1/prenotazioni/piscina/disponibilita/?inventario={id}&data=YYYY-MM-DD` (`AllowAny` via `permission_classes` passato direttamente ad `@action`) restituisce solo i conteggi aggregati residui (`{ombrellone, gazebo, lettino, sdraia}`), nessun dato personale — permette al flusso self-service di mostrare la disponibilità residua senza dover autenticare il cliente per leggere `/piscina/?data=...` (che esporrebbe nome/telefono/note di tutte le prenotazioni del giorno). La stessa logica di aggregazione (anti-overbooking) è condivisa con `PrenotazionePiscinaSerializer.validate()` tramite l'helper `calcola_disponibilita()` in `prenotazioni/utils.py`, per evitare di duplicare il calcolo in due punti che potrebbero divergere.
3.  **Modello `OccupazionePostazione`**
    *   **Scopo:** Assegnazione **giornaliera** di una `struttura.Postazione` a un cliente — chi occupa quell'ombrellone/gazebo in una data specifica, quanti lettini/sdraie ha, ed eventualmente il collegamento alla prenotazione reale. Vive in `prenotazioni` (non in `struttura`) perché è un dato transazionale legato alla `data`, non al layout fisico.
    *   **Campi:** `id`, `postazione` (FK a `struttura.Postazione`, `CASCADE`), `data`, `prenotazione` (FK opzionale a `Prenotazione_Piscina`, `SET_NULL` — può essere assegnata anche manualmente senza una prenotazione reale), `cliente_nome`, `numero_lettini`, `numero_sdraie`, `orario_arrivo_previsto` (`TimeField` opzionale, `null=True, blank=True` — distinto da `Prenotazione.ora`: quello è l'orario della prenotazione originale se esiste, questo è l'orario di arrivo previsto per *questa* postazione/giorno, compilabile anche per i clienti walk-in che non hanno una prenotazione reale a monte), `created_at`, `updated_at`. Vincolo `unique_together = ('postazione', 'data')`: una sola occupazione per postazione al giorno.
    *   **API:** `OccupazionePostazioneViewSet` (`prenotazioni/views.py`) su `/api/v1/prenotazioni/occupazioni-postazione/`, filtrabile per `data`, `postazione` e `postazione__inventario`.
4.  **Modelli `Prenotazione_Tavolo` e `Prenotazione_Asporto`** *(Da sviluppare)*

### App `menu` (Catalogo Prodotti) *(Da sviluppare)*
1.  **Modelli `Prodotto` e `Voce_Ordine`**
    *   **Scopo:** Gestione del catalogo food & beverage e delle righe d'ordine.

---

## 2. Logica di Validazione e Output (Focus Piscina)

### Controllo Orari
Prima del salvataggio, il sistema verifica che l'`ora` richiesta per la prenotazione rientri strettamente tra l'`orario_apertura` e l'`orario_chiusura` definiti nel listino inventario attivo in quel momento.

### Calcolo Disponibilità in Tempo Reale (Anti-Overbooking)
Prima di confermare una `Prenotazione_Piscina`, il sistema (tramite Serializer):
1.  Identifica l'inventario attivo.
2.  Aggrega tramite query (`Sum`) le risorse già prenotate per la `data` richiesta (escludendo gli stati 'CANCELLED' e la prenotazione corrente in caso di update).
3.  Verifica che la nuova richiesta non ecceda la disponibilità residua (es. `richiesti <= totale - gia_prenotati`). Restituisce un errore specifico (400 Bad Request) per la singola risorsa esaurita.

### "Giorno Pieno" — Chiusura Manuale delle Prenotazioni Online (`GiornoPienoPiscina`)
Marcatura manuale per `(inventario, data)`, indipendente dai conteggi anti-overbooking sopra: copre i casi (evento privato, chiusura straordinaria, o semplicemente "non accettiamo altre richieste online oggi") in cui la piscina va considerata al completo anche se i conteggi ombrellone/gazebo/lettino/sdraia avrebbero ancora residuo — inclusi i casi "solo ingresso", che l'anti-overbooking sopra non limita affatto (non esiste un `totale_ingressi` sull'inventario).
*   **Modello:** `id`, `inventario` (FK, `CASCADE`), `data`, `note` (opzionale), `created_at`; `unique_together=('inventario','data')`.
*   **API:** `GiornoPienoPiscinaViewSet` su `/api/v1/prenotazioni/giorni-pieni/` (`IsAuthenticated`, come le altre azioni della mappa staff — filtrabile per `inventario`/`data`). Nessuna azione custom: creare una riga marca il giorno, cancellarla lo sblocca (`list`/`create`/`destroy` standard del `ModelViewSet`).
*   **Effetto — blocca solo il self-service pubblico, mai lo staff:** `PrenotazionePiscinaSerializer.validate()` verifica `request.user.is_authenticated` (via `self.context['request']`, popolato automaticamente da DRF) — se la richiesta è **anonima** (il flusso self-service Area Cliente, sezione 7, che usa `AllowAny` su `create`) e per `(inventario, data)` esiste una riga `GiornoPienoPiscina`, la creazione fallisce con `{"data": "..."}`. Se la richiesta è **autenticata** (staff, es. "+ Nuovo cliente" dalla mappa), il controllo viene saltato: lo staff ha visibilità diretta sulla disponibilità fisica reale e può comunque registrare un walk-in nonostante il flag — il flag serve solo a chiudere il canale online, non a bloccare la struttura.
*   **Esposizione pubblica:** l'azione pubblica `disponibilita` (sopra) include ora anche `"pieno": true/false` nella risposta, oltre ai residui numerici — il flusso self-service (sezione 7) lo legge per mostrare un banner bloccante senza dover tentare una `create()` per scoprirlo.
*   **Frontend staff:** `GiornoPienoToggle.tsx` (`src/components/staff/piscina-mappa/`), montato nella pagina mappa subito sotto `DateNavigator`. Se non impostato: pulsante outline "🔒 Segna giorno come tutto prenotato". Se impostato: banner rosa con pulsante "Rimuovi blocco". Nei **giorni passati** (`isPastDate`, sezione 5) il controllo non è più azionabile (non avrebbe effetto: il self-service non può comunque prenotare nel passato) — se il giorno era stato segnato, resta visibile solo come banner informativo di sola lettura, altrimenti il componente non renderizza nulla. Stato letto/scritto tramite `PiscinaMappaDataContext` (`giornoPieno`, `toggleGiornoPieno`), caricato in parallelo agli altri dati della mappa per la data selezionata.

### Tipologie di Ingresso (Intero / Ridotto Pomeridiano / Bambini a Fascia d'Età / Gratuito)
Oltre all'ingresso a tariffa intera (`prezzo_ingresso`, sempre esistito), tre tariffe **opzionali** aggiuntive: ridotto pomeridiano, bambini (a fascia d'età) e gratuito (sotto l'età minima della fascia bambini). **Decisione di design:** sono semplici contatori con un prezzo proprio (0 per il gratuito), esattamente come ombrellone/gazebo/lettino/sdraia — **nessuna verifica automatica dell'età** (il sistema non ha un'anagrafica età per persona, solo un conteggio aggregato: le soglie `eta_minima_bambino`/`eta_massima_bambino` restano solo **testo guida**, chi compila la prenotazione giudica in autonomia, stesso principio per cui il sistema non verifica che un ombrellone prenotato sia davvero usato). **L'orario ridotto pomeridiano è invece validato davvero** (sotto), perché lega due campi già presenti sulla stessa prenotazione (`ora` e `ingressi_ridotti`) — a differenza dell'età, non richiede dati che il sistema non ha.
*   **Validazione orario ridotto pomeridiano (in entrambe le direzioni):** se `ingressi_ridotti > 0`, l'`ora` della prenotazione deve essere `>= orario_inizio_ridotto` dell'inventario (un ridotto non ha senso prima della soglia). **Complementare** (aggiunta successiva): se `ingressi` (tariffa intera) `> 0` **e** la tariffa ridotta è configurata sull'inventario (`prezzo_ingresso_ridotto > 0`), l'`ora` deve essere `< orario_inizio_ridotto` — dalla soglia in poi un ingresso a tariffa intera non ha più senso economico, andrebbe registrato come ridotto. Il secondo controllo è condizionato al prezzo configurato (a differenza del primo, che si applica sempre) perché se la tariffa ridotta non è mai stata impostata dallo staff, `orario_inizio_ridotto` resta solo il valore di default del campo, non un'alternativa realmente disponibile a cui dirottare il cliente. **Lato backend** (unica fonte di verità, `PrenotazionePiscinaSerializer.validate()`, subito dopo il controllo apertura/chiusura, un blocco per direzione): entrambi risolvono `ingressi`/`ingressi_ridotti` sul valore già presente su `self.instance` quando omessi da un PATCH parziale (stesso pattern già usato per `data`/`ora`/`inventario` in cima al metodo) e rifiutano rispettivamente con `{"ingressi_ridotti": "..."}` / `{"ingressi": "..."}` — verificato anche il caso di un PATCH che sposta `ora` senza toccare i contatori ingressi. **Lato frontend**, stesse regole in `validateOrarioIngressoRidotto()`/`validateOrarioIngressoIntero()` (helper "gemelli" in `utils/piscinaMappa.ts`), usate dagli stessi tre punti che possono creare/modificare i contatori ingresso su una prenotazione: form prenotazione cliente (`app/cliente/piscina/[inventarioId].tsx`, contro `form.orario`), `confirmEditPrenotazione` in `PiscinaSheetsContext.tsx` (contro `editForm.ora`), e `confirmCreateCliente` (contro l'orario di arrivo previsto scelto nel foglio "+ Nuovo cliente", sezione 5 — non più `nowHHMM()` fisso da quando quel campo è diventato editabile). Il secondo controllo (`validateOrarioIngressoIntero`) è invocato solo se `prezzo_ingresso_ridotto > 0`, stesso gate del backend.
*   **`struttura.PiscinaInventario` — nuovi campi:** `prezzo_ingresso_ridotto`/`prezzo_ingresso_bambino` (Decimal, default `0.00` — **`0.00` = tariffa non configurata**, nascosta ovunque nei form/prezzi finché lo staff non imposta un valore `> 0`, stesso trattamento di un `totale_*` a 0 per ombrellone/gazebo/lettino/sdraia). `orario_inizio_ridotto` (TimeField, default `14:00`) è la soglia indicativa per il ridotto pomeridiano. La tariffa bambini è una **fascia d'età** `[eta_minima_bambino, eta_massima_bambino]` (PositiveSmallInteger, default `3`/`12`): dentro la fascia paga `prezzo_ingresso_bambino`, **sotto `eta_minima_bambino` l'ingresso è indicativamente gratuito**. Tutti editabili dallo staff nel form listino insieme ai prezzi.
*   **`prenotazioni.PrenotazionePiscina` — nuovi campi:** `ingressi_ridotti`/`ingressi_bambini`/`ingressi_gratuiti` (PositiveSmallInteger, default `0`), accanto al campo `ingressi` preesistente (che resta la tariffa intera). `ingressi_gratuiti` è comunque un contatore reale (non solo un flag): serve a tenere il conto di quanti bambini sotto l'età minima sono inclusi, anche se il loro prezzo è 0. Nessun vincolo tra i quattro contatori, nessun `totale_ingressi*` sull'inventario (stesso motivo per cui `ingressi` non è mai stato incluso nell'anti-overbooking, sezione sopra: `calcola_disponibilita()` continua a ignorare tutti e quattro i contatori ingresso).
*   **Frontend — un solo punto di verità per mostrare/nascondere le tariffe:** `PREZZO_ITEMS` (`src/utils/prezzi.ts`) ha un flag `hideIfZero` (alternativo a `totaleKey`, che invece gated su una quantità): le voci "Ridotto pomeridiano"/"Bambini" compaiono nella griglia prezzi lato cliente (`app/cliente/piscina.tsx`) solo se il prezzo relativo è `> 0`; sotto la griglia, una didascalia mostra la fascia d'età e la soglia gratuita solo quando la tariffa bambini è configurata. Lo stesso controllo (`Number.parseFloat(inventario.prezzo_ingresso_bambino) > 0`) è ripetuto puntualmente ovunque un form debba mostrare/nascondere i campi quantità "bambini"/"gratuiti" corrispondenti (non essendoci un componente form condiviso tra le sedi elencate sotto): form prenotazione cliente (`app/cliente/piscina/[inventarioId].tsx`, `RisorsaField` — esteso con un prop `gratis?: boolean` che mostra "Gratis" al posto del prezzo per il campo ingressi gratuiti, l'unico senza un `prezzo_*` proprio da leggere), `NewClienteSheet.tsx` e `EditPrenotazioneSheet.tsx` lato staff (entrambi leggono `inventario` da `usePiscinaMappaData()` per le label con soglia/fascia, es. "Ingressi bambini (3-12 anni)" / "Ingressi gratuiti (sotto 3 anni)").
*   **Riepiloghi con breakdown:** `formatIngressiSummary()` (helper in `utils/piscinaMappa.ts`) genera la stringa "🎟️ N 🌇 N 🧒 N 🆓 N" (interi sempre mostrati anche a 0, le altre tre solo se `> 0`) — usato da `ClientiDelGiornoSheet.tsx` e dalla scheda cliente (`app/staff/clienti/[clienteId].tsx`). `SoloIngressoPanel.tsx` mostra invece un conteggio aggregato (somma di tutti e quattro i contatori), non il breakdown. Il biglietto PDF (`ticket_piscina.html`, sotto) elenca le quattro righe separatamente.
*   **Form listino staff (`PiscinaInventarioSection.tsx`):** sottosezione "Ingressi ridotti (opzionali)" sotto "Prezzi (€)" — prezzo ridotto pomeridiano + soglia oraria (`orario_inizio_ridotto` con la stessa maschera `formatOrarioInput` usata da tutti gli altri campi orario testuali dell'app, sezione 7), poi prezzo bambini + **due** campi età (`eta_minima_bambino`/`eta_massima_bambino`, entrambi `NumberField` numerici) con una didascalia dinamica che ricalcola a video la fascia e la soglia gratuita mentre lo staff digita ("Fascia 3-12 anni: paga il prezzo bambini. Sotto 3 anni: ingresso gratuito."). Un testo esplicito chiarisce che le soglie sono solo indicative e che un prezzo a 0 nasconde la tariffa.
    *   **Bug corretto — campi disallineati:** ogni riga a due colonne (`HStack space="sm"`, entrambi i figli `flex-1`) è alta quanto il figlio più alto — se un'etichetta è più lunga dell'altra e va a capo (es. "Prezzo ridotto pomeridiano" contro "Valido dalle", o "Età minima (sotto: gratis)" contro "Età massima"), il campo con l'etichetta corta finisce spinto più in basso, disallineato rispetto all'altro. **Non risolto con `numberOfLines`** sull'etichetta (stessa gotcha nota altrove nel progetto: il `Text` di gluestack-ui ha un'implementazione web separata che non lo filtra, causando il warning React "does not recognize the numberOfLines prop", sezione 5) — risolto invece accorciando le etichette che rischiavano di andare a capo ("Prezzo ridotto pomeridiano" → "Prezzo ridotto", "Età minima (sotto: gratis)" → "Età minima", il dettaglio "sotto: gratis" resta comunque nella didascalia sotto la riga) così restano tutte su una riga in una colonna da metà larghezza. Sistemata anche l'incoerenza dei campi "isolati" (`Sdraia` e `Prezzo bambini`, gli unici due prezzi con un numero dispari di elementi nella loro sezione): prima erano `NumberField` fuori da un `HStack`, quindi si allargavano a piena larghezza invece che a metà come tutti gli altri — ora sono in un `HStack` con un `Box` vuoto (`className="flex-1"`) come secondo figlio, così occupano solo la colonna sinistra e la griglia a due colonne resta visivamente coerente in tutto il form.

### Generazione Biglietto d'Ingresso (PDF)
*   **Libreria:** `WeasyPrint` con template HTML dedicato (`prenotazioni/templates/ticket_piscina.html`).
*   **Trigger e permessi:** `GET /api/v1/prenotazioni/piscina/{id}/scarica_biglietto/` (`PrenotazionePiscinaViewSet.scarica_biglietto`) è **pubblico** (`AllowAny`, tramite `permission_classes` sull'`@action`, stesso pattern di `disponibilita`): il cliente self-service (Area Cliente, sezione 7) non ha un token JWT e deve poter scaricare il biglietto della propria prenotazione subito dopo averla effettuata, quando è ancora `PENDING`. L'unico "segreto" richiesto è l'UUID v4 della prenotazione (non enumerabile, a differenza di un ID incrementale) — stesso principio di molti link di conferma ordine "a capacità" (chi ha l'URL vede il biglietto). Bloccato (`400`) solo per le prenotazioni **`CANCELLED`**; disponibile sia per `PENDING` sia per `CONFIRMED`.
*   **Contenuto e Vincoli:** Il documento mostra un badge di stato (ambra "In attesa di conferma dello staff in biglietteria" per `PENDING`, verde "Prenotazione confermata" per `CONFIRMED`), il nome del cliente, data, ora e il riepilogo dettagliato delle risorse prenotate. **Non include alcun prezzo**, fungendo esclusivamente da titolo di accesso/riepilogo richiesta. Una prenotazione `PENDING` genera comunque il PDF: funge da riepilogo da mostrare in biglietteria, dove lo staff la conferma fisicamente.

### Infrastruttura API
*   **CORS:** Configurati tramite `django-cors-headers` per permettere la comunicazione incondizionata con il frontend React Native.
*   **Filtri:** Implementati tramite `django-filter` per consentire query complesse in GET (es. ricerca per `data`, `stato` e `cliente_id`).

---

## 3. Autenticazione Frontend (Axios + Context)

Lo strato di autenticazione vive in `le-gole-fe/src/` ed è composto da tre moduli:

1.  **`utils/storage.ts`** — wrapper su `expo-secure-store` per salvare/leggere/cancellare `accessToken` e `refreshToken` nel keychain/keystore del device.
2.  **`services/api.ts`** — istanza Axios condivisa (`baseURL` puntato al backend Django) con due interceptor:
    *   **Request:** inietta automaticamente `Authorization: Bearer <accessToken>` letto da `storage.ts`.
    *   **Response:** su un 401 (esclusi gli endpoint `/v1/users/login/*`), avvia una singola chiamata di refresh a `/v1/users/login/refresh/` (condivisa tra richieste 401 concorrenti tramite promise cache), salva la nuova coppia access/refresh — dato che `ROTATE_REFRESH_TOKENS` è attivo lato backend — e ripete la richiesta originale. Se il refresh fallisce (refresh token scaduto/in blacklist), pulisce i token e notifica l'app tramite `setSessionExpiredHandler`.
3.  **`context/AuthContext.tsx`** — espone `AuthProvider` e l'hook `useAuth()` (`isAuthenticated`, `isLoading`, `user`, `login(username, password)`, `logout()`).
    *   Al mount, se esiste un `accessToken` salvato, chiama `GET /api/v1/users/staff/me/` per ripristinare la sessione.
    *   `login()` chiama `POST /api/v1/users/login/`, salva i token e poi recupera il profilo dallo stesso endpoint `me/`.
    *   È registrato come `onSessionExpired` handler di `api.ts`, così una sessione scaduta lato server pulisce anche lo stato React.
    *   **`StaffUser`** rispecchia i campi di `UtenteSerializer` (`id, username, email, is_superuser, is_active`): `is_superuser` è usato per mostrare/nascondere la sezione "Gestione Staff" (vedi sezione 6), lato UI, come controparte del gate `IsSuperUser` già imposto dal backend.

Il layout radice `app/_layout.tsx` (Expo Router) avvolge l'intera app in `<GluestackUIProvider mode="light"><AuthProvider>...</AuthProvider></GluestackUIProvider>`.

---

## 4. UI Kit Frontend: gluestack-ui v5 + NativeWind

Lo styling del frontend è basato su **NativeWind v5** (Tailwind CSS v4) con i componenti prefabbricati di **gluestack-ui v5 (alpha)**, integrati tramite la CLI ufficiale (`npx gluestack-ui@latest add <componente>`), che copia il codice sorgente del componente in `le-gole-fe/components/ui/<componente>/` (pattern "copy-paste", non una libreria da import diretto: i componenti sono file di progetto, modificabili liberamente).

1.  **Configurazione**
    *   `tailwind.config.js` — il `content` include `app/**`, `src/**`, **`components/**`** e **`node_modules/@gluestack-ui/**`** (necessario perché NativeWind processi le classi usate dai componenti gluestack-ui).
    *   `global.css` — definisce i design token come variabili CSS RGB (`--background`, `--foreground`, `--primary`, `--border`, `--destructive`, ecc.) in tre blocchi: `:root` (default light), `@media (prefers-color-scheme: dark)` e le classi `:root.dark` / `:root.light` (a specificità più alta, usate per il toggle manuale via `GluestackUIProvider`). I token sono poi esposti come colori Tailwind (`bg-background`, `text-foreground`, `bg-primary`, ecc.) tramite il blocco `@theme inline`.
    *   `babel.config.js` / `metro.config.js` — preset `nativewind/babel` e `withNativewind(config, { inlineRem: 16 })`, invariati rispetto al setup standard.
2.  **Tema forzato Light**
    *   L'app **non supporta il tema scuro**: `GluestackUIProvider` in `app/_layout.tsx` è impostato con `mode="light"` (non `"system"`), il che forza sia il colore native (`Appearance.setColorScheme`) sia la classe `.light` su web, ignorando le preferenze di sistema dark.
    *   Sfondo dell'app: **`#eae0c8`** (crema tenue, desaturato per non risultare troppo acceso), impostato una sola volta come `--background` in `global.css` (`:root` e `:root.light`) e propagato automaticamente a tutte le pagine tramite la classe utility `bg-background`.
3.  **Componenti installati** (in `components/ui/`): `box`, `button` (+ `ButtonText`, `ButtonSpinner`, `ButtonIcon`, `ButtonGroup`), `heading`, `hstack`, `input` (+ `InputField`, `InputIcon`, `InputSlot`), `spinner`, `text`, `vstack`, oltre al `gluestack-ui-provider` generato dall'`init`.
    *   **Nota:** il template generato per `spinner/index.tsx` usava l'API deprecata `nativeStyleToProp` con `target: 'style'`, incompatibile con la versione di `react-native-css` installata (errore di tipo). Corretto in `nativeStyleMapping` con `target: false`, che mappa il colore risolto dalla className direttamente alla prop `color` dello `ActivityIndicator` invece che a un attributo di stile.
4.  **Pagine convertite** a usare i componenti gluestack-ui invece dei primitivi React Native grezzi: `app/index.tsx` (selezione ruolo, con card centrata; il pulsante "Area Cliente" naviga ora a `/cliente/index`, vedi sezione 8), `app/login.tsx` (login staff) e `app/staff/index.tsx` (home staff).
5.  **Logo reale — due varianti in `assets/`, entrambe PNG 1600×1600 con vero canale alpha** (verificato via PIL, `alpha.getextrema()` → `(0, 255)`, non un semplice sfondo pieno): stesso artwork (cerchio con montagne/fiume, "LE GOLE — Osteria - Pizzeria"), solo il colore del tratto cambia. **Nessuna versione con sfondo opaco esiste più nel progetto** — entrambe vanno scelte in base al contrasto col contesto, non ce n'è una "di default":
    *   **`logo-le-gole-nero.png`** — tratto nero, per sfondi chiari. Usato in `app.json` (`icon`, `android.adaptiveIcon.foregroundImage` — quest'ultimo è anche l'uso "corretto" per un'icona adattiva Android, che si aspetta un foreground con aree trasparenti composte dal sistema sopra il `backgroundColor`) e in tutti i punti chiari dell'interfaccia:
        *   `app/index.tsx` (selezione ruolo) e `app/login.tsx` (login staff) — logo centrato sopra il titolo (96×96 e 64×64, `resizeMode="contain"`), su sfondo `bg-card` chiaro.
        *   `app/staff/_layout.tsx` — l'header sostituisce il badge circolare con iniziali testuali "LG" con il logo, dentro un `Box` (stesse dimensioni del badge precedente, `h-9 w-9 md:h-10 md:w-10`) e `Image resizeMode="cover"`.
        *   `app/cliente/index.tsx` — l'hero sostituisce l'emoji 🌴 nel badge circolare (`h-16 w-16 bg-white/70`) con il logo, stesso pattern.
    *   **`logo-le-gole-bianco.png`** — tratto bianco, l'unica leggibile sul footer scuro. Usato **solo** in `src/components/cliente/ClienteFooter.tsx` (sfondo `#332a18`), al posto dell'emoji 🌴 precedente: renderizzato direttamente (`Image` 28×28, `resizeMode="contain"`), **senza alcun chip/contenitore dietro** — con la variante nera qui sarebbe servito un chip chiaro per il contrasto (tentativo precedente, poi scartato non appena resa disponibile la variante bianca: più semplice usare il colore giusto per il contesto che simulare il contrasto con un badge).
    *   **`favicon.png`** — **non fornito dall'utente, generato** compositando `logo-le-gole-nero.png` su uno sfondo bianco pieno (script Python one-off con Pillow, `Image.alpha_composite`, non un asset da rigenerare a mano se il logo cambia — va rieseguito). Usato solo da `app.json` → `web.favicon`: la tab del browser mostra il logo su un riquadro bianco anche in tema scuro, a differenza di un favicon con sfondo trasparente che con un tratto nero rischierebbe di sparire su una tab bar scura. Verificato che il dev server Metro lo rispecchia davvero (richiesta a `/favicon.ico`, convertita al volo da PNG, alpha piatto a 255 e pixel d'angolo bianco).
    *   Caricati via `require('.../assets/logo-le-gole-{nero,bianco}.png')`, nessuna libreria immagini aggiuntiva oltre a `Image` di `react-native`. In precedenza `app.json` puntava a file (`icon.png`, `favicon.png`, gli `android-icon-*.png`) che **non esistevano affatto** in `assets/` — una configurazione già rotta, sistemata insieme all'introduzione dei loghi reali. `android.adaptiveIcon.backgroundImage`/`monochromeImage` sono stati rimossi (stessi file inesistenti); resta solo `backgroundColor` + `foregroundImage`, combinazione valida per lo schema Expo.
    *   **Nome app/tab del browser:** `expo.name` in `app.json` è ora `"Le Gole"` (era rimasto al valore di default generato da Expo, `"le-gole-fe"`, che comparira anche come `<title>` della pagina web) — `slug` resta `"le-gole-fe"` (identificatore di progetto, non il nome visualizzato, non c'è motivo di cambiarlo).

---

## 5. Mappa Postazioni Piscina (Card Inventario + Pagina Mappa)

1.  **Home Staff (`app/staff/index.tsx`)**: saluto contestuale all'orario (Buongiorno/Buon pomeriggio/Buonasera/Buonanotte) + sottotitolo che spiega lo scopo della pagina. Sezioni in ordine: `PiscinaInventarioSection`, `StaffManagementSection` (sezione 6).
    *   **Ricerca clienti (`app/staff/clienti.tsx`):** l'entry point **non** è più una sezione nella home (rimossa `ClientiSearchSection.tsx`), ma un'icona 🔍 nell'header persistente `app/staff/_layout.tsx` (accanto al logo/nome "Le Gole", stesso `HStack` del pulsante account — `Pressable` senza bordo né sfondo, stessa sobrietà visiva del pulsante account adiacente), visibile su **tutte** le pagine staff e non solo nella home. La pagina di ricerca vera e propria resta invariata: non un `Actionsheet` ma una pagina dedicata (header con freccia indietro circolare, stesso stile di `MappaHeader.tsx`), perché i risultati possono essere una lista lunga da scorrere comodamente, a differenza delle scelte rapide in altri fogli dell'app. Un singolo campo di ricerca testuale matcha **sia nome sia telefono** (nessuna scelta di campo richiesta allo staff): query **debounced** (300ms) e non inviata sotto i 2 caratteri (`MIN_QUERY_LENGTH`, per non scaricare l'intera anagrafica a ogni tasto premuto), con stati distinti per "digita per iniziare", "nessun risultato per «query»" e la lista dei risultati (nome + telefono, non più cliccabile per chiamare — l'intera card è invece un `Pressable` che naviga alla scheda dettaglio, sotto). Backend: `ClienteViewSet.get_queryset()` (`users/views.py`) applica `Q(nome__icontains=search) | Q(telefono__icontains=search)` quando è presente `?search=` — un OR fra i due campi, non ottenibile con `filterset_fields`/`DjangoFilterBackend` di default (fa AND per campo), quindi filtrato manualmente invece di dichiarare un `filterset_class` dedicato. Azione riservata allo staff (permessi di default della viewset, `IsAuthenticated`), a differenza di `create` che resta `AllowAny` per il flusso self-service (sezione 1).
        *   **Bug corretto — scroll orizzontale su tutte le pagine staff:** il `Pressable` dell'icona 🔍 aveva dimensione fissa `h-9 w-9` (36px) ma al suo interno, per un refuso, conteneva anche un testo grezzo "Cerca" (non avvolto in `<Text>`) dentro un `Box` con `pr-5` — contenuto molto più largo dei 36px del pulsante, che traboccava fuori dai suoi limiti allargando l'intera riga dell'header oltre la viewport e causando lo scroll orizzontale su ogni pagina staff (l'header è condiviso, sezione 6). Corretto lasciando che il `Pressable` cresca in larghezza col contenuto (`h-9 flex-row items-center gap-1`, senza `w-9` fisso) invece di forzarlo in un riquadro troppo piccolo, ed etichetta "Cerca" ora un vero `<Text size="sm" className="font-medium text-sky-700">` accanto all'icona, non più testo grezzo. **Difesa aggiuntiva in `global.css`:** `html, body { overflow-x: hidden; background-color: rgb(var(--background)); }` — senza queste due regole il body del browser resta bianco di default, quindi un qualunque elemento che in futuro trabocchi anche di pochi pixel oltre la viewport mostrerebbe di nuovo lo scroll orizzontale e una striscia bianca (lo sfondo del `<body>`, non quello crema dell'app) invece di essere semplicemente tagliato.
    *   **Scheda cliente (`app/staff/clienti/[clienteId].tsx`):** rotta dinamica raggiunta toccando una card nei risultati di ricerca, coesiste con il file piatto `app/staff/clienti.tsx` come `app/staff/piscina/[inventarioId].tsx` coesiste con `app/staff/index.tsx` (sezione 7). Header identico alle altre pagine staff (freccia indietro + `router.back()`); una card mostra il telefono come unico link `tel:` diretto tramite `Linking.openURL` (spostato qui dalla lista risultati, che ora naviga soltanto — evita di annidare due `Pressable`, stesso principio di "Torna a oggi" in `DateNavigator.tsx`). Sotto, lo **storico prenotazioni piscina** del cliente: `getCliente(id)` (`GET /v1/users/clienti/{id}/`) e `listPrenotazioniPiscinaByCliente(id)` (`GET /v1/prenotazioni/piscina/?cliente_id={id}` — il filtro `cliente_id` è già in `filterset_fields` su `PrenotazionePiscinaViewSet`, nessuna modifica backend necessaria) vengono caricati in parallelo; l'elenco è **ordinato per data decrescente** (più recente per prima) via confronto lessicografico client-side su `data`/`ora` (entrambi formati ordinabili come stringa, "YYYY-MM-DD"/"HH:MM:SS" — nessun `ordering` configurato lato backend, `DEFAULT_FILTER_BACKENDS` include solo `DjangoFilterBackend`). Ogni riga mostra data (`formatDateDDMMYYYY`, nuovo helper in `utils/piscinaMappa.ts`) + ora, un badge di stato colorato (🟡 `PENDING`/🟢 `CONFIRMED`/🔴 `CANCELLED`, stesso pattern "soft badge" `bg-*-100`/`text-*-700` di `StaffManagementSection.tsx`), le note se presenti e il riepilogo risorse (🎟️/⛱️/⛺/🛏️/🪑, solo le risorse > 0), stesso stile di riga di `ClientiDelGiornoSheet.tsx`. Include **tutte** le prenotazioni (anche passate e `CANCELLED`), a differenza della mappa staff che mostra solo quelle del giorno selezionato.
2.  **Card Inventario (`src/components/staff/PiscinaInventarioSection.tsx`)**
    *   `PiscinaInventarioCard`: header con icona/nome/badge "Attivo", griglia 2×2 per le 4 risorse (ombrelloni/gazebi/lettini/sdraie).
    *   La zona "vai alla mappa" (header + risorse + pillola evidenziata "Gestisci mappa postazioni →") è un `Pressable` che naviga a `/staff/piscina/[inventarioId]`; i pulsanti "Modifica listino"/"Elimina" sono **fuori** da questo `Pressable`, separati da un divisorio, così non c'è ambiguità tra "apri la mappa" e "modifica il listino" (in precedenza erano annidati nello stesso `Pressable`).
    *   Stato di caricamento con `Spinner`; stato vuoto con icona, copy e CTA "+ Crea il primo listino".
    *   **Selettore tipo inventario:** ogni pulsante "+ Aggiungi" (header e stato vuoto) apre prima un `Actionsheet` di scelta tra **Piscina** / **Asporto** / **Sala** (`TIPO_INVENTARIO_OPTIONS`). Solo Piscina è implementata: sceglierla apre il form esistente. Asporto e Sala hanno badge "Presto" e, se toccate, mostrano solo un alert "in arrivo" — **non esistono ancora modelli/API backend per questi due tipi**, è un placeholder di UI in attesa di definire i campi necessari (numero tavoli/capienza per Sala, cosa rappresenta un "inventario" per Asporto).
3.  **Pagina Mappa (`app/staff/piscina/[inventarioId].tsx`)** — prima rotta dinamica del progetto (Expo Router, `useLocalSearchParams`).
    *   **Vista legata alla data:** selettore ◀ / "Oggi" / ▶ che carica postazioni, prenotazioni e occupazioni per il giorno selezionato (default: oggi). Toccando il testo della data (`DateNavigator.tsx`) si apre un `Actionsheet` con un calendario mensile (`CalendarPicker`, sotto) per saltare direttamente a un giorno lontano invece di scorrere ±1 giorno alla volta; il pulsante "Torna a oggi" (visibile solo quando la data selezionata non è oggi) è ora un `Pressable` **sibling**, non annidato dentro quello che apre il calendario — nidificare due `Pressable` avrebbe rischiato un doppio trigger su web (`react-native-web` fa risalire i click DOM al genitore). L'etichetta della data mostra **"Oggi"** invece della data estesa (es. "Dom 26 Lug") quando `selectedDate` coincide con la data odierna, per coerenza con lo stesso pulsante "Torna a oggi" che scompare in quel caso.
    *   **Sola lettura per i giorni passati:** `isPastDate` (`PiscinaMappaDataContext`, confronto `toISODate(selectedDate) < toISODate(new Date())`) blocca ogni modifica quando si sta consultando un giorno precedente a oggi — le `Postazione` sono una risorsa **strutturale condivisa da tutte le date** (sezione 1), quindi spostarle o eliminarle mentre si guarda lo storico modificherebbe anche il presente/futuro senza che sia ovvio allo staff. Bloccati: drag (`PostazioneMarker` riceve `readOnly={isPastDate}` — su past date il marker ignora ogni spostamento e tratta sempre il gesto come un tap, mai come un drag, sia nel ramo `PanResponder` nativo sia in quello Pointer Events web), assegnazione di un cliente a una postazione libera (`confirmAssign`), modifica di un occupante già assegnato (`confirmOccupantEdit`), liberazione (`liberaPostazione`) ed eliminazione di una postazione (`handleDeletePostazione`) — quest'ultima bloccata anche se vuota, per lo stesso motivo delle posizioni. Il tap su un marker resta invece sempre attivo (anche nei giorni passati): apre comunque `PostazioneSheet` per **consultare** l'occupante, con un banner ambra "Sola lettura" e gli input/pulsanti disabilitati (`AssignPostazioneForm.tsx`/`OccupantForm.tsx` leggono `isPastDate` da `usePiscinaSheets()`). Ogni funzione di scrittura in `PiscinaSheetsContext` ripete comunque il controllo come backstop difensivo (non solo il disabled lato UI), dato che non c'è alcun vincolo lato backend su questo — è una restrizione puramente della UI staff.
        *   **Estesa anche a creazione postazioni/clienti e a modifica/eliminazione prenotazioni:** stesso `isPastDate`, stesso principio (dato transazionale/storico non alterabile a posteriori). I pulsanti "+ Aggiungi postazione" e "+ Nuovo cliente" (`ActionToolbar.tsx`) sono entrambi `disabled` (stile sbiadito) quando `isPastDate`; `confirmAddPostazione`/`confirmCreateCliente` ripetono il controllo come backstop (anche "+ Nuovo cliente" crea sempre una prenotazione per la data selezionata sulla mappa, quindi ricade nello stesso blocco delle altre scritture legate a `selectedDate`). Le icone ✏️/🗑️ di ogni riga in `ClientiDelGiornoSheet.tsx` (modifica/elimina prenotazione) ricevono `readOnly={isPastDate}` e diventano `disabled` (sbiadite, nessun tap) — a differenza del tap sul marker sopra, qui non si apre nemmeno un foglio di sola consultazione: non c'è nulla da consultare che non sia già visibile nella riga stessa. `EditPrenotazioneSheet.tsx`/`NewClienteSheet.tsx` mostrano comunque (la prima) un banner ambra "Sola lettura" e disabilitano il rispettivo pulsante di submit come ulteriore backstop (i fogli in teoria non si aprono più per un giorno passato, essendo le rispettive icone/pulsanti disabilitati, ma restano raggiungibili in astratto); `confirmEditPrenotazione`/`handleDeletePrenotazione`/`confirmCreateCliente` in `PiscinaSheetsContext` ripetono il controllo lato funzione.
    *   **Storico posizione postazioni (`prenotazioni.PostazionePosizioneStorico`):** dato che `Postazione.pos_x`/`pos_y` è condiviso da tutte le date, senza uno storico spostare una postazione in un giorno modificabile faceva "saltare" retroattivamente anche la sua posizione nei giorni passati già consultati (bug osservato dopo l'introduzione della sola lettura sopra — un primo tentativo aveva aggiunto uno snapshot solo su `OccupazionePostazione`, poi scartato perché copriva solo le postazioni occupate quel giorno, non quelle libere). Modello dedicato: `postazione` (FK), `data` (giorno da cui la posizione è diventata effettiva), `pos_x`/`pos_y`, `unique_together=('postazione','data')` — **una riga per giorno in cui la posizione è cambiata**, non una riga per postazione per giorno (una postazione mai spostata resta con una sola riga per sempre). Vive in `prenotazioni` (non in `struttura`) per lo stesso motivo di `OccupazionePostazione`: è un dato legato alla data, non al layout fisico in sé (sezione 1).
        *   **Scrittura:** `PostazioneViewSet.perform_create`/`perform_update` (`struttura/views.py`) chiamano `registra_posizione_storico()` (`prenotazioni/utils.py`) dopo ogni salvataggio, che fa un `update_or_create` sulla riga di **oggi** (data server-side, `timezone.localdate()`, mai quella del client) — così più drag nello stesso giorno aggiornano la stessa riga invece di accumularne una per movimento, e i giorni passati (dove il drag è comunque disattivato lato UI, vedi sopra) non vengono mai toccati.
        *   **Lettura:** `PostazioneViewSet.list()` accetta un parametro opzionale `?data=YYYY-MM-DD`. Se la data richiesta è **passata** (`< timezone.localdate()`), sovrascrive `pos_x`/`pos_y` di ogni postazione nella risposta con `posizioni_effettive()` (`prenotazioni/utils.py`: prende, per ciascuna postazione, l'ultima riga di storico con `data <=` quella richiesta — se non ce n'è nessuna, la postazione non compare nel dict e il chiamante ricade sulla posizione live). Per oggi/il futuro (o quando `data` è omesso) risponde con la posizione live di `Postazione`, invariata — **nessun impatto sui chiamanti esistenti che non passano `data`**. Lato frontend, `listPostazioni(inventarioId, data)` (`src/services/struttura.ts`) passa sempre `toISODate(selectedDate)`; `MappaCanvas` non ha bisogno di alcun override locale, disegna semplicemente `postazioni` così come arriva dal backend.
        *   **Backfill:** la migrazione che introduce il modello (`prenotazioni/0003_postazioneposizionestorico.py`) semina una riga per ogni `Postazione` già esistente, ancorata alla sua `created_at` con la posizione **attuale** (l'unica nota — non esiste uno storico reale pregresso da recuperare); da quel momento in poi ogni spostamento è tracciato accuratamente.
    *   **Soft delete di `Postazione` (`deleted_at`):** stesso problema di fondo dello storico posizione, ma per l'**esistenza** stessa della postazione — aggiungerne o eliminarne una oggi non deve alterare cosa risultava presente sulla mappa nei giorni passati già consultati. `Postazione.deleted_at` (`DateTimeField` nullable, migrazione `struttura/0002_alter_postazione_unique_together_and_more.py`) sostituisce l'eliminazione reale: `PostazioneViewSet.destroy()` valorizza solo `deleted_at = timezone.now()` invece di fare un `DELETE` SQL — altrimenti, dato che `OccupazionePostazione.postazione` e `PostazionePosizioneStorico.postazione` sono entrambe `CASCADE`, cancellare davvero la riga avrebbe **distrutto lo storico occupazioni/posizioni** di ogni giorno passato in cui quella postazione era stata assegnata, non solo fatta sparire dalla mappa.
        *   **Vincolo di unicità condizionale:** `numero` deve restare unico per inventario solo **tra le postazioni attive** (`UniqueConstraint(fields=['inventario','numero'], condition=Q(deleted_at__isnull=True))`, non più un semplice `unique_together`) — altrimenti eliminare l'ombrellone #5 impedirebbe per sempre di riassegnare quel numero a una nuova postazione. I `UniqueConstraint` condizionali non vengono introspezionati automaticamente da DRF in un validator, quindi `PostazioneSerializer.validate()` replica lo stesso controllo lato applicativo (per un 400 leggibile invece di un `IntegrityError` grezzo dal DB).
        *   **Viste correnti vs vista storica:** `PostazioneViewSet.get_queryset()` filtra sempre `deleted_at__isnull=True` (create/retrieve/update/list "live" non vedono mai le postazioni eliminate). Il ramo storico di `list()` (quando `data` è una data passata) interroga invece `Postazione.objects` **non filtrato** direttamente: include le postazioni eliminate **dopo** quella data (esistevano ancora allora) tramite `Q(deleted_at__isnull=True) | Q(deleted_at__date__gt=data)`, ed esclude quelle create **dopo** quella data tramite `created_at__date__lte=data` — ricostruendo così l'esatto insieme di postazioni presenti sulla mappa quel giorno, non quello attuale. `self.filter_queryset()` viene comunque applicato a questo queryset "manuale" per rispettare `inventario`/`tipo` da `filterset_fields` come nel ramo live.
        *   **`PostazioneSerializer`:** `exclude = ['deleted_at']` (non più `fields = '__all__'`) — è un dettaglio implementativo interno, non va esposto al frontend.
    *   **Riepilogo disponibilità del giorno:** riga di 4 tessere (ombrelloni/gazebi/lettini/sdraie) con `residui / totale`, calcolate sottraendo dal totale dell'inventario la somma delle risorse già prenotate per la data selezionata (stessa logica anti-overbooking di `prenotazioni/serializers.py`, non conta solo le postazioni fisicamente piazzate sulla mappa). Colore dinamico: verde (disponibile), ambra (residuo ≤ 20% del totale), rosso (esaurito).
    *   **Canvas zoomabile:** area fissa (1000×560 unità logiche) con pulsanti "－"/"＋"/reset che applicano una `scale` (0.6-2.4×) via `transform`; il canvas è racchiuso in due `ScrollView` innestate (orizzontale + verticale) per il pan quando ingrandito.
    *   **Drag & drop postazioni:** ogni icona `Postazione` (⛱️ ombrellone, ⛺ gazebo) è trascinabile. **Attenzione:** la versione di `react-native-web` in uso non implementa più il responder system legacy su cui si basa `PanResponder` — su web gli handler vengono semplicemente ignorati (nessun tap, nessun drag). Il marker usa quindi due percorsi distinti nello stesso componente: **Pointer Events** (`onPointerDown` + listener `window` su `pointermove`/`pointerup`) su web, `PanResponder` nativo su iOS/Android — nessuna dipendenza aggiuntiva. Al rilascio la nuova posizione (`pos_x`/`pos_y`) viene salvata via `PATCH /postazioni/{id}/`.
        *   **Affidabilità del tap:** la soglia che distingue un tap da un drag (`TAP_MOVE_THRESHOLD_PX`) è 8px, non 3-4px — una persona raramente clicca con 0px di movimento tra mousedown e mouseup, e una soglia troppo stretta classificava tap reali come drag, impedendo l'apertura del form. I `Text` (emoji + numero) dentro il marker hanno anche `pointer-events-none select-none`: senza, un mousedown che parte esattamente sopra il glyph può innescare la selezione/trascinamento nativo del testo del browser invece del tap sul marker.
        *   **Nome cliente sotto il marker:** una postazione occupata mostra un cartellino (bordo verde smeraldo, coerente col cerchio dell'icona quando occupata) con il nome del cliente assegnato (`OccupazionePostazione.cliente_nome`) subito sotto l'icona — non c'era prima alcun modo di sapere chi occupasse una postazione senza aprirne il foglio di dettaglio. `PostazioneMarker` riceve `clienteNome` da `MappaCanvas` (`occupazioneByPostazione.get(postazione.id)?.cliente_nome`); il cartellino è un `Box` **sibling** del cerchio del marker (non annidato al suo interno: il cerchio, `ICON_SIZE`, è troppo piccolo per contenere un nome leggibile), `pointer-events-none` (il tap sulla postazione resta gestito solo dal cerchio) e segue lo stesso offset di trascinamento temporaneo (`drag.dx/dy`) del marker così non si stacca durante un drag. Larghezza fissa (`NAME_LABEL_WIDTH`, non scalata con lo zoom a differenza dei font-size); i nomi lunghi sono troncati con ellissi **manualmente in JS** (`truncateNome()`, taglio a `NAME_LABEL_MAX_CHARS`), non con la prop `numberOfLines` di React Native — il componente `Text` di gluestack-ui ha due implementazioni parallele (`components/ui/text/index.tsx` nativo che avvolge il vero `Text` di RN, `index.web.tsx` che è un semplice `<span>`, stesso pattern già noto per `components/ui/icon/` in sezione 7): su web `numberOfLines` non è una prop riconosciuta da uno `<span>` e finisce spatasciata come attributo DOM grezzo (warning React "does not recognize the numberOfLines prop"). Il troncamento manuale evita del tutto la dipendenza da un comportamento che diverge tra le due piattaforme.
    *   **Assegnazione cliente — multi-unità:** se un cliente ha prenotato più di un ombrellone e/o più di un gazebo, va assegnato una volta per unità prenotata (2 ombrelloni ⇒ 2 postazioni ombrellone distinte). `remainingByPrenotazione` (nella pagina mappa) conta, per ogni prenotazione, quante `OccupazionePostazione` sono già collegate **distinte per tipo** (guardando `Postazione.tipo` di ogni occupazione), e calcola i residui `{ombrellone, gazebo}` separatamente — non basta un flag booleano "già assegnata/non assegnata". `daAssegnare` include una prenotazione finché ha almeno un'unità residua di un tipo qualsiasi; il pannello "Da assegnare" e il picker in-sheet mostrano i residui aggiornati (non i totali prenotati) e si filtrano per il tipo della postazione target (un cliente con solo gazebo residuo non compare come selezionabile per una postazione ombrellone). Dopo un'assegnazione, se il cliente ha ancora unità residue dello stesso tipo, resta selezionato per velocizzare le assegnazioni successive.
        *   **Limite lettini/sdraie per postazione (bug corretto):** in precedenza il foglio di assegnazione precompilava i campi "Lettini"/"Sdraie" con il **totale** prenotato (`Prenotazione.lettino`/`sdraia`) invece che con il residuo, e non c'era alcuna validazione — assegnando più di una postazione allo stesso cliente si potevano sommare più lettini/sdraie di quelli effettivamente prenotati, senza che il pannello "Da assegnare" (che mostra correttamente i residui, essendo comunque reattivo a `remainingByPrenotazione`) desse alcun avviso in tal senso. `handlePickCliente`/`handleMarkerPress` (`PiscinaSheetsContext.tsx`) ora precompilano con il **residuo** (`remainingByPrenotazione.get(id).lettino/sdraia`), non il totale. `confirmAssign`/`confirmOccupantEdit` calcolano `maxLettini`/`maxSdraie` (residuo della prenotazione collegata, `+` il valore già presente sull'occupazione in modifica quando si sta editando — perché `remainingByPrenotazione` lo conta già come "usato") e rifiutano il salvataggio se il valore digitato lo supera; `null` (nessun limite) solo per un'occupazione **non** collegata a nessuna prenotazione reale (assegnazione manuale, `OccupazionePostazione.prenotazione` è opzionale). `AssignPostazioneForm.tsx`/`OccupantForm.tsx` mostrano il limite accanto all'etichetta del campo ("Lettini (max N)").
    *   **Creazione cliente + prenotazione al volo — sezione separata:** un pulsante "+ Nuovo cliente" accanto a "Clienti del giorno" (sopra il pannello "Da assegnare") apre un `Actionsheet` dedicato (`NewClienteSheet.tsx`, diviso in due riquadri "Dati cliente"/"Cosa prenota", con etichette icona + asterisco rosso sui campi obbligatori — stesso linguaggio visivo del form di prenotazione cliente self-service, sezione 7) con nome, telefono, **note aggiuntive opzionali** (`Prenotazione.note`, inviate insieme alla prenotazione creata subito dopo, non all'anagrafica cliente), **orario di arrivo previsto** (campo testuale obbligatorio con la stessa maschera `formatOrarioInput` degli altri campi orario dell'app, precompilato con `computeDefaultOrario(null, selectedDate)` — "adesso" se si sta operando su oggi, vuoto per una data futura) e le stesse quantità di una prenotazione vera (ingressi/ombrelloni/gazebi/lettini/sdraie). Alla conferma (`confirmCreateCliente` in `PiscinaSheetsContext`) l'orario viene validato con lo stesso `validateOrarioArrivo()` usato dal foglio di assegnazione postazione (obbligatorio, non nel passato se la data è oggi), poi vengono creati **in sequenza** un vero record `users.Cliente` (`POST /api/v1/users/clienti/`) e una vera `Prenotazione_Piscina` collegata (`POST /api/v1/prenotazioni/piscina/`, `data` = giorno selezionato sulla mappa, `ora` = l'orario scelto, **`stato: 'CONFIRMED'`** — creata direttamente dallo staff, quindi già confermata a prescindere dall'orario: può essere "adesso" per un walk-in fisicamente presente oppure un orario successivo dello stesso giorno) — non più un `Cliente` "nudo" senza alcuna prenotazione a monte. **Prima di questa modifica** l'orario era sempre fissato a `nowHHMM()` (nessun campo editabile): ora che è un campo del form, il controllo sull'orario ridotto pomeridiano (`validateOrarioIngressoRidotto`, sezione 2) confronta l'orario scelto con la soglia invece dell'ora attuale. La nuova prenotazione passa dal medesimo controllo anti-overbooking di `PrenotazionePiscinaSerializer.validate()` di qualsiasi altra prenotazione. Se ha almeno un'unità di ombrellone o gazebo, resta **selezionata** (`selectPrenotazioneCandidate(created.id)`) esattamente come un chip scelto dal pannello "Da assegnare"; se è "solo ingresso" (`ombrellone === 0 && gazebo === 0`) non viene selezionata (non ci sarebbe nulla da piazzare sulla mappa) e compare direttamente nel pannello "Solo ingresso" e in "Clienti del giorno", come qualunque altra prenotazione. Non esiste più un concetto di "walk-in" distinto dalle prenotazioni normali: un cliente creato dalla mappa rispetta gli stessi vincoli di tipo/quantità ombrellone-gazebo di uno arrivato da qualsiasi altro canale, tramite lo stesso `remainingByPrenotazione`/`daAssegnare` già usati per tutte le prenotazioni.
    *   **Form di assegnazione:** tap su una postazione libera apre un `Actionsheet` che mostra sempre lo stesso "select-like" (`Pressable` che apre un secondo `Actionsheet` con `ActionsheetItem` — nessun componente `select` di gluestack-ui installato, stessa logica del selettore tipo inventario) con le prenotazioni del giorno che hanno residui > 0 per il tipo della postazione target, mostrando nome **e telefono** (`PrenotazionePiscinaSerializer.cliente_telefono`) e le unità ancora da assegnare — comprese quelle appena create con "+ Nuovo cliente", indistinguibili dalle altre. La scelta precompila nome/telefono/lettini/sdraie e collega `OccupazionePostazione.prenotazione`. Selezionare prima un chip dal pannello "Da assegnare" ha lo stesso effetto. Se non ci sono prenotazioni con residui per quel tipo, niente form — solo un messaggio ("Nessun cliente in attesa per questa postazione...") che rimanda al pannello "Da assegnare" o al pulsante "+ Nuovo cliente"; il pulsante "Assegna" non compare finché non c'è un candidato valido, "Elimina postazione" resta sempre disponibile.
        *   **Orario di arrivo previsto:** campo **obbligatorio** (`OccupazionePostazione.orario_arrivo_previsto` non è più nullable a livello di modello/migrazione). Nel foglio **"occupant"** (modifica di una postazione già assegnata, `OccupantForm.tsx`) resta un `Input` editabile, validato al salvataggio con `validateOrarioArrivo` (formato "HH:MM", non vuoto, e — solo se la data selezionata è oggi — non nel passato rispetto a quello attuale): qui rappresenta l'orario di arrivo *effettivo* per quella specifica postazione/giorno, che lo staff può correggere manualmente in tempo reale.
            *   **Sola visualizzazione nel foglio di assegnazione (`AssignPostazioneForm.tsx`):** dato che l'orario è ormai sempre scelto a monte (dal cliente nel form self-service, o dallo staff nel form "+ Nuovo cliente"/modifica prenotazione — entrambi sezione 7/qui sopra), il campo nel foglio di **assegnazione** di una postazione libera non è un `Input` editabile ma un riquadro di sola lettura (icona orologio + valore, con didascalia "Impostato dall'orario scelto al momento della prenotazione — non modificabile da qui"). `handlePickCliente`/`handleMarkerPress` (ramo `pren`, in `PiscinaSheetsContext.tsx`) precompilano `sheetForm.orarioArrivo` con `formatTime(pren.ora)` **esatto**, senza alcun ricalcolo — **non** passa più da `computeDefaultOrario()` (che invece ricadrebbe su "adesso" se `pren.ora` risultasse già nel passato). **Bug corretto:** la versione precedente usava `computeDefaultOrario()` anche qui, quindi se lo staff assegnava la postazione qualche minuto dopo l'orario dichiarato (es. prenotato per le 14:30, assegnato alle 14:35 — situazione normalissima), la postazione mostrava "14:35" mentre la lista clienti/la prenotazione stessa mostravano ancora "14:30": due orari diversi per la stessa prenotazione, senza alcuna reale divergenza intenzionale. Di conseguenza `confirmAssign` non usa più `validateOrarioArrivo` (che avrebbe respinto un orario "nel passato" — qui non ha senso, dato che il valore è ereditato e non scelto in quel momento): valida solo che sia un "HH:MM" ben formato (`parseHHMMToMinutes`), sufficiente perché `Prenotazione.ora` è un campo obbligatorio già validato (incluso il vincolo sulla soglia del ridotto pomeridiano, sezione 2) alla creazione/modifica della prenotazione a monte.
            *   **Parametro `orarioMinimo` di `computeDefaultOrario()` (`utils/piscinaMappa.ts`), non più invocato:** clampa il default calcolato in modo che non scenda mai sotto una soglia (pensato per non proporre un orario precedente a `orario_inizio_ridotto` per un cliente con ingressi ridotti). Da quando il foglio di assegnazione mostra `Prenotazione.ora` in sola lettura (punto sopra), `handlePickCliente`/`handleMarkerPress` non passano più da `computeDefaultOrario()` (l'orario lì è ereditato, già validato a monte) — nessun chiamante attuale passa più questo terzo argomento. Resta nella firma della funzione (nessun altro punto ne ha bisogno oggi, ma riproporlo in futuro — es. un campo orario editabile in un nuovo flusso — richiederebbe la stessa soglia).
        Tap su una postazione occupata permette di modificarne i dati (incluso l'orario di arrivo) o "liberarla" (qui il nome resta testo libero, non passa dal picker).
        *   **Accessibilità dialog:** entrambi gli `Actionsheet` di questa pagina passano un `aria-label` esplicito a `ActionsheetContent` (dinamico in base a `sheetMode` per il foglio principale). Il rilevamento automatico del titolo di react-aria (usato internamente dal componente `Actionsheet` di gluestack-ui) via `<Heading>` interno è soggetto a una race condition con l'animazione di apertura del foglio — il warning "A dialog must have a title" in console appariva ad ogni apertura anche quando un `Heading` era presente; l'`aria-label` esplicito lo evita perché react-aria lo controlla per primo, prima di scansionare il DOM alla ricerca di un heading.
    *   **Pannello "Da assegnare":** prenotazioni del giorno con almeno un'unità residua (ombrellone e/o gazebo) non ancora assegnata; i chip mostrano i residui aggiornati, non i totali prenotati — oltre a ⛱️/⛺ mostrano anche i residui di 🛏️ lettini e 🪑 sdraie non ancora assegnati (calcolati sottraendo, per ciascuna prenotazione, la somma di `numero_lettini`/`numero_sdraie` di tutte le sue `OccupazionePostazione` collegate dal totale `lettino`/`sdraia` prenotato — stesso schema per-prenotazione già usato per ombrellone/gazebo in `remainingByPrenotazione`).
    *   **Pannello "Solo ingresso":** prenotazioni del giorno con `ombrellone === 0 && gazebo === 0` — mai mostrate sulla mappa, solo in questa lista di sola lettura.
    *   **Lista "Clienti del giorno":** pulsante accanto al titolo "Da assegnare" che apre un terzo `Actionsheet` (`aria-label="Clienti del giorno"`, stesso pattern di accessibilità) con **tutte** le prenotazioni del giorno selezionato (non solo quelle con residui), ordinate per nome. Ogni riga mostra nome, l'**orario previsto della prenotazione** (`Prenotazione.ora`, badge 🕐), telefono, le eventuali **note della prenotazione** (`note`, badge 📝, mostrato solo se non vuoto), la riga **"Prenotato:"** con **tutte** le risorse prenotate (🎟️ ingressi sempre, ⛱️/⛺/🛏️/🪑 solo se > 0 — i totali della prenotazione, non i residui) e un badge: **"Completo"** (verde) se non ha più unità residue di ombrellone/gazebo — vale anche per le prenotazioni "solo ingresso", sempre complete perché non prevedono postazioni — oppure **"Da assegnare"** (ambra) con il dettaglio delle unità residue (ombrellone/gazebo/lettini/sdraie) su una riga separata sotto "Prenotato:", stesso conteggio del pannello "Da assegnare".
        *   **Selezione per assegnazione:** toccare la parte informativa di una riga la seleziona come `selectedPrenotazioneId` (stesso effetto di un chip del pannello "Da assegnare") e chiude la lista, per assegnarla subito toccando una postazione libera sulla mappa — **solo se la prenotazione non è "Completo"**: un cliente già completamente assegnato non ha più unità da piazzare, quindi il tap sulla sua riga non fa nulla (in precedenza selezionava comunque la prenotazione, facendo comparire erroneamente il suggerimento "Tocca una postazione libera sulla mappa per assegnarla" anche se non c'era nulla da assegnare).
        *   **Modifica/elimina prenotazione:** ogni riga ha due icone impilate verticalmente sulla destra (non affiancate: più compatte e leggibili in una riga già densa di informazioni), ✏️ (apre un quarto `Actionsheet`, `aria-label` dinamico con il nome del cliente, e chiude la lista sottostante per evitare due fogli sovrapposti) e 🗑️ (con conferma `window.confirm`/`Alert.alert`, stesso pattern di `handleDeletePostazione`). Il foglio di modifica cambia solo i **dati della prenotazione** (orario, note, ingressi, ombrellone, gazebo, lettini, sdraie, tutti in un'unica `PATCH /v1/prenotazioni/piscina/{id}/`) — nome e telefono restano readonly qui perché vivono sull'anagrafica `Cliente` condivisa, non sulla singola prenotazione. L'eliminazione fa un `DELETE` reale della prenotazione: `PrenotazionePiscinaViewSet.destroy()` (`prenotazioni/views.py`) elimina esplicitamente, in una `transaction.atomic()`, anche tutte le `OccupazionePostazione` collegate prima di cancellare la prenotazione — senza questo intervento il vincolo `SET_NULL` le avrebbe lasciate "occupate" sulla mappa, solo scollegate. Le postazioni tornano quindi davvero libere; il frontend rispecchia subito la pulizia filtrando `occupazioni` in locale, senza aspettare un refetch.
        *   **Conferma di una prenotazione self-service `PENDING` (bug corretto):** prima di questa modifica **non esisteva alcuna azione lato staff per far passare una prenotazione da `PENDING` a `CONFIRMED`** — nessun punto del flusso (assegnare una postazione, modificarne i dati, la mappa in generale) cambiava mai lo stato, quindi ogni prenotazione arrivata dal flusso self-service Area Cliente (sezione 7, che nasce sempre `PENDING`) restava in attesa a tempo indeterminato anche dopo essere stata gestita fisicamente in struttura. `confirmPrenotazione()` (`PiscinaSheetsContext`) chiama `editPrenotazione(id, { stato: 'CONFIRMED' })`; è raggiungibile da due punti: un'icona ✅ aggiuntiva (solo se `stato === 'PENDING'`, prima di ✏️/🗑️) su ogni riga di `ClientiDelGiornoSheet.tsx`, e un banner ambra con pulsante "Conferma prenotazione" dentro `EditPrenotazioneSheet.tsx` quando la prenotazione aperta è `PENDING`. Entrambi i punti rispettano `isPastDate` (stesso backstop di modifica/eliminazione, sopra) e mostrano uno spinner mentre la richiesta è in volo (`confirmingPrenotazioneId` in `PiscinaSheetsContext`, confrontato con l'id della prenotazione). Ogni riga di `ClienteDelGiornoRow` mostra ora anche un badge di stato colorato (🟡 In attesa/🟢 Confermata/🔴 Cancellata, helper condivisi `STATO_PRENOTAZIONE_LABEL`/`STATO_PRENOTAZIONE_BADGE` in `utils/piscinaMappa.ts`, riusati anche dalla scheda cliente in `app/staff/clienti/[clienteId].tsx` che li definiva prima localmente) accanto al badge "Assegnato"/"Da assegnare" — prima d'ora lo stato PENDING/CONFIRMED non era visibile da nessuna parte in questa lista, solo nella scheda cliente.
        *   **Bug del validate() sui PATCH parziali:** `PrenotazionePiscinaSerializer.validate()` leggeva `data.get('inventario')`/`data.get('data')` assumendo sempre un payload completo; su un `PATCH` parziale (come il form di modifica qui sopra, che invia solo i campi cambiati) questi risultavano `None`, causando un 500 quando si confrontava `ora` con `inventario.orario_apertura`. Corretto facendo ricadere ogni campo omesso sul valore già presente su `self.instance` quando la request è un update parziale.
    *   **Stile pulsanti:** i pulsanti `variant="outline"` di gluestack-ui di default (`border-border` + `bg-background`) risultano poco visibili sopra gli sfondi `bg-sky-*` di questa pagina. Le icone di navigazione/zoom e il tasto indietro usano quindi `border-2 border-sky-300 bg-white shadow-sm`; i pulsanti distruttivi nei fogli (`Elimina postazione`, `Libera postazione`) usano `border-2 border-destructive bg-destructive/10` invece del semplice `border-destructive/40`; "+ Aggiungi postazione" usa il variant `default` (solido) invece di `outline`.
    *   **Posizione pulsanti azione:** "+ Nuovo cliente" (`variant="outline"`, `border-emerald-400 bg-emerald-50` — stesso tono verde smeraldo del chip walk-in selezionato) vive accanto a "+ Aggiungi postazione" in una `HStack` subito sotto il canvas della mappa: entrambi creano una nuova entità (postazione/cliente) indipendente dalla data selezionata, quindi stanno insieme come un'unica toolbar di azioni, non dentro il pannello "Da assegnare". "Clienti del giorno →", invece, resta accanto al titolo "Da assegnare" (è un filtro/vista sui dati di *quel* giorno) ma come `Button` bordato (`border-2 border-sky-300 bg-white shadow-sm`, stesso stile delle icone di navigazione) invece di un semplice `Text` sottolineato — in precedenza entrambi i pulsanti erano link di testo affiancati nell'header del pannello, poco leggibili come azioni.
    *   **Nuovi service frontend:** `src/services/struttura.ts` estesa con CRUD `Postazione`; nuovo `src/services/prenotazioni.ts` con `listPrenotazioniPiscina` e CRUD `OccupazionePostazione`.
    *   **`src/components/shared/CalendarPicker.tsx`** — calendario scritto da zero (nessuna libreria di calendario/date-picker installata, solo primitive gluestack-ui): navigazione ◀/▶ indipendente dal giorno selezionato, griglia settimanale lunedì-domenica (`src/utils/calendar.ts`: `buildMonthGrid`/`buildWeekGrid`, `addMonths`/`addWeeks`, `formatMonthLabel`/`formatWeekLabel`, `startOfMonth`/`startOfWeek`, `WEEKDAY_LABELS` — puramente di calcolo griglia, senza dipendenze dal dominio piscina, a differenza di `utils/piscinaMappa.ts`), giorno di oggi cerchiato, giorno selezionato evidenziato pieno. Due prop opzionali lo rendono riutilizzabile sia lato staff sia lato cliente pur con esigenze diverse: `minDate` (i giorni prima vengono disabilitati/sbiaditi — usato solo lato cliente, sezione 7, che non può prenotare nel passato; omesso lato staff, che deve poter consultare liberamente anche le date passate) e `countsByDate`/`onVisibleMonthChange` (mappa `{ISODate: numero}`, con un callback invocato ad ogni cambio di periodo visibile per richiedere solo i conteggi del mese/dei mesi coinvolti — usato solo lato staff, sotto).
        *   **Vista Mese / Settimana (solo staff):** un toggle a pillola ("Mese" / "Settimana") appare **solo quando è passata la prop `countsByDate`** (quindi mai lato cliente, che non la passa) — è la stessa prop che già distingueva staff/cliente, riusata anche per decidere se offrire il toggle, invece di introdurre un booleano dedicato ridondante. In vista Mese la griglia è quella consueta (5-6 settimane); in vista Settimana `buildWeekGrid` mostra solo i 7 giorni della settimana contenente `selectedDate`, con header "◀ 20 - 26 Lug 2026 ▶" al posto del nome del mese. Passare da una vista all'altra riallinea sempre il periodo visibile al giorno attualmente selezionato (`switchToMonth`/`switchToWeek` richiamano `startOfMonth`/`startOfWeek` su `selectedDate`).
        *   **Conteggio come badge in alto a destra del numero del giorno, visibile sia in Mese sia in Settimana:** `CalendarDayCell` renderizza il badge in overlay sul cerchio del giorno (reso `relative`, badge `absolute` in alto a destra) invece che sotto — terza iterazione dello stile di questo indicatore: prima badge in overlay classico su una versione precedente (scartato perché il numero copriva parzialmente la data), poi pillola separata sotto il cerchio (scartata su richiesta esplicita, stile non gradito), infine questo badge "a goccia" staccato dal cerchio tramite un anello `border-background` (sempre cream qui, dato che `CalendarPicker` vive solo dentro un `Actionsheet` con `bg-background`) che crea un effetto "ritagliato" invece che sovrapposto. Non occupando spazio extra nella riga (a differenza della vecchia pillola sotto il cerchio), il badge è leggero abbastanza da restare visibile anche sull'intera griglia mensile, non solo in vista Settimana come nella versione precedente. **Dimensioni diverse per vista Mese vs Settimana:** il cerchio del giorno è più piccolo in vista Mese (`h-9 w-9` contro `h-11 w-11`) e le righe sono più ravvicinate, quindi il badge lì usa un'**altezza fissa** più contenuta (`h-3.5` contro `h-5`, non solo `min-w`: un'altezza fissa rende l'ingombro verticale prevedibile anche per i conteggi a due cifre, che allargano solo la larghezza), bordo più sottile (`border` invece di `border-2`) e un offset più marcato (`-right-2 -top-2` contro `-right-1.5 -top-1.5`) — nella prima versione, con lo stesso offset/bordo della vista Settimana, i conteggi a due cifre (es. "23") finivano per coprire parzialmente la cifra del giorno sottostante nel cerchio più piccolo del mese; verificato visivamente (Playwright, screenshot ravvicinato sulla singola cella) sia il fix sia che la vista Settimana continuasse a funzionare correttamente. Colorato in base al conteggio con la stessa scala verde/ambra/rosso usata altrove nel progetto per la disponibilità (`DisponibilitaCards`): 1-2 → emerald, 3-5 → amber, 6+ → rose. **Stile badge "soft"** (`badgeClassesForCount`, `bg-*-100` + `text-*-700`, non sfondo pieno saturo con testo bianco): stesso linguaggio visivo dei badge di stato già usati altrove nell'app (es. "Superuser"/"Disattivo" in `StaffManagementSection.tsx`, "Attivo" nelle card inventario) — oltre a essere più coerente stilisticamente, risolve anche un problema di contrasto reale: `bg-amber-500` pieno con testo bianco è sotto la soglia WCAG AA per testo piccolo (~2:1), mentre `bg-amber-100`/`text-amber-700` supera ampiamente la soglia. Il numero è centrato con `justify-center` sul badge **e** `text-center` + `leading-none` sul `Text` stesso — non basta l'`items-center` del contenitore: lo stile base di `Text` su web include `text-start`/`display-inline` (`components/ui/text/styles.tsx`), quindi senza `text-center` esplicito il numero risulta non perfettamente centrato orizzontalmente nel badge. La logica di rendering della singola cella (`CalendarDayCell`) è stata estratta in un componente a parte per tenere sotto controllo la complessità del componente principale.
        *   **Settimane a cavallo di due mesi:** `onVisibleMonthChange` in vista Settimana può scattare per **due mesi diversi** nella stessa sessione di navigazione (es. settimana 27 luglio - 2 agosto richiede sia i conteggi di luglio sia quelli di agosto). `DateNavigator.handleVisibleMonthChange` unisce (`{...prev, ...result}`) i conteggi ricevuti invece di sovrascriverli, altrimenti la seconda richiesta cancellerebbe i conteggi del primo mese già mostrati sulla settimana visibile.
    *   **Conteggio prenotazioni per giorno (calendario staff):** `DateNavigator.tsx` passa a `CalendarPicker` una mappa `countsByDate` popolata da `getConteggiPrenotazioniPiscina()` (nuovo, in `src/services/prenotazioni.ts`) verso `GET /api/v1/prenotazioni/piscina/conteggi/?inventario={id}&anno=&mese=` — nuova azione su `PrenotazionePiscinaViewSet` (riservata allo staff, permessi di default della viewset) che restituisce `{ISODate: numero}` sparso (conteggio delle prenotazioni non cancellate di quel mese per quell'inventario, giorni assenti = 0), **incluse le date passate** — uno staff può così vedere a colpo d'occhio quanti clienti ci sono stati/ci sono in ogni giorno del mese, anche prima di oggi, senza dover cambiare `selectedDate` giorno per giorno per scoprirlo. La query di aggregazione (`values('data').annotate(totale=Count('id'))`, esclude `CANCELLED`) è isolata nella viewset, non condivisa con `calcola_disponibilita()` in `prenotazioni/utils.py` (quest'ultima calcola *residui* per singola data/risorsa ai fini anti-overbooking, non un conteggio per-giorno su un intero mese: scopi diversi, niente da fattorizzare in comune).

---

## 6. Gestione Staff e Autenticazione Self-Service (Frontend)

Sezione visibile solo ai superuser, più tre schermate pubbliche di attivazione/reset password raggiunte via deep link/email.

1.  **`src/services/staff.ts`** — CRUD e azioni custom su `/api/v1/users/staff/`, un binding 1:1 con le rotte di `UtenteViewSet` (vedi sezione 1):
    *   `listStaff` (`GET /`), `createStaff` (`POST /`, solo `{username, email}`), `updateStaff` (`PATCH /{id}/`, solo `{username, email}` — la password non è più un campo di questo endpoint), `deleteStaff` (`DELETE /{id}/`).
    *   `setStaffActive(id, is_active)` (`PATCH /{id}/set-active/`).
    *   `activateAccount`, `confirmPasswordReset` (stesso payload `{uid, token, password}`, endpoint diversi: `activate/` vs `reset-password/confirm/`), `requestPasswordReset` (`POST reset-password/request/`).
    *   **Nota:** il cambio password da autenticato (`change_password`) è stato rimosso interamente, backend incluso — non solo il pulsante "Cambia password" nel menu account dello staff (`app/staff/_layout.tsx`) e il componente `ChangePasswordSheet.tsx`/schema `changePasswordSchema`/funzione `changePassword` lato frontend, ma anche l'azione `change_password` su `UtenteViewSet` e `ChangePasswordSerializer` lato backend (nessun altro chiamante, verificato prima di eliminarli): il flusso "Password dimenticata?" copre già lo stesso caso d'uso, ed è raggiungibile da `app/login.tsx` senza dover essere già autenticati.
2.  **Validazione form (Zod):**
    *   `src/schemas/staff.ts` → `staffSchema` (`username` ≥ 3 caratteri, `email` valida), usato sia in creazione sia in modifica dello staff (niente più campo password in questi form).
    *   `src/schemas/auth.ts` → `setPasswordSchema` (password ≥ 8 caratteri + conferma, usato da attivazione e reset), `forgotPasswordSchema` (email). La validazione di lunghezza/robustezza è solo un primo filtro: il backend rivalida sempre con `django.contrib.auth.password_validation.validate_password`.
    *   `src/utils/zodErrors.ts` → `formatZodErrors`, condivisa da entrambi gli schema file, riduce gli `issues` di Zod a `{ campo: primo messaggio }` per mostrarli sotto il rispettivo input.
3.  **`src/context/StaffManagementContext.tsx`** — carica l'elenco staff al mount (`listStaff`) ed espone le promise CRUD (`addStaff`, `editStaff`, `removeStaff`, `toggleStaffActive`) con aggiornamento ottimistico dello state locale dopo ogni risposta, stesso pattern dei context della mappa piscina (sezione 5).
4.  **`src/components/staff/StaffManagementSection.tsx`** — gate lato UI: renderizza `null` se `user.is_superuser` è `false` (il backend blocca comunque con `IsSuperUser`, questo è solo evitare di mostrare una sezione che darebbe 403 su ogni azione). Elenco staff con badge 👑 "Superuser", 🏷️ "Tu" (riga dell'utente loggato) e 🔴 "Disattivo" (`!is_active`). Ogni riga (tranne quella dell'utente loggato, per evitare un lockout) ha pulsanti "Modifica", "Disattiva"/"Riattiva" (`toggleStaffActive`) ed "Elimina" (`removeStaff`, con conferma `window.confirm`/`Alert.alert`, stesso pattern usato altrove nel progetto).
5.  **`src/components/staff/StaffFormSheet.tsx`** — form di creazione/modifica (solo username + email). In creazione, dopo il submit riuscito il foglio **non si chiude subito**: mostra un pannello di conferma ("Email di attivazione inviata a {email}") perché il nuovo account non ha ancora una password utilizzabile finché lo staff non completa l'attivazione.
6.  **`src/components/auth/SetPasswordForm.tsx`** — form condiviso (password + conferma, validato con `setPasswordSchema`) usato sia dalla schermata di attivazione sia da quella di reset password; riceve `uid`/`token` dai query params e la funzione `onSubmit` da chiamare (`activateAccount` o `confirmPasswordReset`) come prop, così la UI resta identica e la sola differenza è quale endpoint colpire e i testi mostrati. Se `uid`/`token` mancano mostra un messaggio di "link non valido" invece del form.
7.  **Rotte pubbliche (Expo Router, `app/`):**
    *   `app/activate.tsx` — raggiunta da `legole://activate?uid=...&token=...` (email di attivazione), usa `SetPasswordForm` + `activateAccount`.
    *   `app/reset-password.tsx` — raggiunta da `legole://reset-password?uid=...&token=...` (email di reset), usa `SetPasswordForm` + `confirmPasswordReset`.
    *   `app/forgot-password.tsx` — form con solo l'email, chiama `requestPasswordReset` e mostra sempre lo stesso messaggio di conferma (coerente con la risposta generica del backend, che non rivela se l'email è registrata). Raggiungibile dal link "Password dimenticata?" aggiunto in `app/login.tsx`.
    *   **Nota deep link:** questi URL `scheme://` (scheme `legole`, da `app.json`) funzionano da build standalone o dev client; **non** funzionano invariati da Expo Go, dove il formato è `exp://<host>:8081/--/<path>`. Per testare l'intero flusso email→app durante lo sviluppo serve una build reale, oppure va aperta manualmente la rotta con i query param corretti.

---

## 7. Area Cliente (Landing Cliente Finale)

Prima schermata lato client, raggiunta dal pulsante "Area Cliente" in `app/index.tsx` (ora attivo, naviga a `/cliente`). Nessuna autenticazione richiesta: è un'area pubblica distinta da `app/staff/` (niente `useAuth()`/redirect).

1.  **Struttura rotte (Expo Router):** nuovo gruppo `app/cliente/` con `_layout.tsx` (semplice `<Slot />`, nessun header/chrome condiviso — a differenza di `app/staff/_layout.tsx` non serve gestire sessione/account, quindi non aggiunge nulla oltre a rendere `/cliente` una route registrata) e due schermate:
    *   `app/cliente/index.tsx` — landing: hero (`Box` `rounded-3xl border-sky-200 bg-sky-100`) con icona 🌴 in un badge circolare, titolo di benvenuto e un breve paragrafo esplicativo, seguito da un titolo di sezione ("I nostri servizi") + `ServiziClienteSection`, un banner informativo (`bg-amber-50`/`border-amber-200`, stesso pattern del banner prezzi in `app/cliente/piscina.tsx`) che rimanda allo staff in biglietteria per assistenza, e un `BackButton` (sotto) per tornare alla schermata precedente.
    *   `app/cliente/piscina.tsx` — elenco delle piscine con **listino attivo** (`PiscinaInventario.isActive`, stesso significato lato staff, vedi sezione 5): carica tutti gli inventari con `listPiscinaInventari()` (`GET /v1/struttura/inventario-piscina/`, permessi `IsAuthenticatedOrReadOnly` lato backend — la lettura è pubblica, nessun token richiesto) e filtra client-side su `isActive`. Ogni card mostra nome, descrizione, orari e — tramite `PrezziGrid` — **tutti** i prezzi impostati dallo staff (`prezzo_ingresso/ombrellone/gazebo/lettino/sdraia`, stessi campi del form staff in `PiscinaInventarioSection.tsx`, sezione 5), non solo l'ingresso come in precedenza: una griglia a due colonne (stesso pattern visivo di `STOCK_ITEMS` nella card staff) con icona/etichetta/prezzo per riga, tramite gli helper condivisi `PREZZO_ITEMS`/`formatPrezzo` in `src/utils/prezzi.ts`. Ogni prezzo di una risorsa (ombrellone/gazebo/lettino/sdraia) compare solo se il relativo `totale_*` dell'inventario è `> 0` — non ha senso mostrare "Gazebo €4" se quella piscina non ha gazebi. Il tap su una card naviga a `app/cliente/piscina/[inventarioId].tsx` (il vero flusso di prenotazione, sotto). Stato vuoto ("Al momento non ci sono piscine disponibili") gestito sia per il caso di zero listini attivi sia come rete di sicurezza se lo stato cambia tra il caricamento della landing e la navigazione qui. Subito sotto il titolo, un banner informativo fisso (`bg-amber-50`/`border-amber-200`, icona ℹ️) avvisa che i prezzi mostrati potrebbero variare in base all'orario d'ingresso e rimanda al ristorante o alla biglietteria per maggiori informazioni — un chiarimento sui prezzi listati, non legato a un singolo inventario.
    *   `app/cliente/piscina/[inventarioId].tsx` — **flusso di prenotazione self-service**: dati cliente, scelta data/orario, disponibilità residua e scelta risorse, con creazione reale di `Cliente` + `Prenotazione_Piscina` (chiude il punto lasciato aperto in sezione 8). Coesiste con il file `app/cliente/piscina.tsx` allo stesso modo in cui `app/staff/piscina/[inventarioId].tsx` coesiste con `app/staff/index.tsx` (Expo Router, come Next.js, instrada un file piatto `piscina.tsx` per il path esatto e una sottocartella `piscina/[id].tsx` per i path dinamici, senza conflitto).
        *   **Navigazione data:** stesso stile a frecce ±1 giorno della mappa staff (sezione 5), con la freccia "precedente" disabilitata quando `selectedDate` è già oggi — un cliente non può prenotare per una data passata (a differenza della mappa staff, che può navigare liberamente anche nel passato per consultare lo storico). Toccando il testo della data si apre lo stesso `Actionsheet` con `CalendarPicker` (`src/components/shared/CalendarPicker.tsx`, sezione 5) usato lato staff, ma con `minDate` impostato a oggi (giorni passati disabilitati/sbiaditi in griglia) e senza `countsByDate` — il cliente non deve vedere quante prenotazioni ci sono già negli altri giorni, solo scegliere il proprio.
        *   **Disponibilità residua pubblica, senza le 4 card riepilogative:** ad ogni cambio di `inventarioId`/`selectedDate` chiama comunque `getDisponibilitaPiscina()` (in `src/services/prenotazioni.ts`) verso l'azione pubblica `GET /piscina/disponibilita/` (vedi sopra), ma **non** mostra più le tessere colorate riepilogative (quelle restano solo lato staff, tramite `DisponibilitaCards`/`DisponibilitaRow` nella mappa — sezione 5): sulla pagina cliente il residuo di ogni risorsa compare invece **in linea**, accanto al prezzo, dentro il campo quantità stesso (`RisorsaField`, locale al file: "€4 · Residui: 10") — un solo punto dove guardare invece di due. Ogni campo quantità (ombrellone/gazebo/lettino/sdraia) resta nascosto se quella risorsa non esiste per l'inventario (`totale_* === 0`, stesso criterio di `PrezziGrid`).
        *   **Giorno al completo (`disponibilita.pieno`, sezione 2):** se la risposta di `getDisponibilitaPiscina()` per la data selezionata ha `pieno: true` (giorno marcato manualmente dallo staff, `GiornoPienoToggle` sezione 5), l'intero form di prenotazione ("I tuoi dati" + "Cosa vuoi prenotare" + pulsante di invio) viene sostituito da un banner rosa 🔒 "Giorno al completo" — niente residui per risorsa da controllare, il blocco vale per l'intera giornata a prescindere dalle quantità richieste (anche una prenotazione "solo ingresso" viene bloccata). Il selettore data (`DateNav`) resta comunque utilizzabile per provare un altro giorno. Il backend rifiuterebbe comunque la `POST` con lo stesso identico blocco (`PrenotazionePiscinaSerializer.validate()`, sezione 2) — questo banner è solo per non far compilare inutilmente il form a un giorno già chiuso.
        *   **Giorni al completo evidenziati nel calendario:** oltre al banner sul giorno già selezionato sopra, il `CalendarPicker` (sezione 5) usato dal picker data del cliente marca visivamente **in anticipo**, prima ancora della selezione, quali giorni del mese visibile sono al completo — evita di dover navigare giorno per giorno per scoprirlo. Nuovo prop `fullDates?: Record<string, boolean>` su `CalendarPicker`, popolato da `DateNav.handleVisibleMonthChange` (`GET /giorni-pieni/calendario/?inventario=&anno=&mese=` — azione **pubblica** su `GiornoPienoPiscinaViewSet`, nessun altro dato oltre alle date, stesso principio di `disponibilita`) e unito (`{...prev, ...result}`) ai mesi già noti, stesso pattern di merge di `countsByDate` lato staff (sezione 5). Nella cella del giorno (`CalendarDayCell`), un giorno "pieno" è **non selezionabile** (stesso trattamento dei giorni prima di `minDate`, non solo un colore diverso — al cliente non serve poterlo scegliere solo per vedersi poi il banner dopo l'invio) e visivamente distinto con `bg-rose-100` + testo rosa barrato (`line-through`), diverso dal badge numerico ambra/verde/rosso di `countsByDate` (quello resta solo lato staff: `fullDates` è indipendente da `countsByDate` e **non** abilita il toggle Mese/Settimana, legato solo alla presenza di `countsByDate`). Una didascalia sotto il titolo dell'`Actionsheet` ("I giorni evidenziati in rosa sono già al completo") spiega il significato del nuovo colore, dato che prima d'ora il calendario cliente non aveva alcuna codifica colore.
        *   **Form "I tuoi dati" con icone e indicazioni:** ogni campo ha un'icona (👤 nome, `PhoneIcon` telefono, 📝 note, `ClockIcon` orario) e i campi obbligatori sono marcati con un asterisco rosso (nome, telefono, orario — le note restano "(opzionale)"). Sotto il campo Telefono un suggerimento fisso spiega perché conviene usare sempre lo stesso numero: è la chiave con cui `ClienteViewSet.create()` fa l'upsert (sezione 1), quindi un numero coerente è ciò che permette allo staff di ritrovare lo storico/le preferenze di un cliente che torna. Il campo Orario mostra sotto di sé l'intervallo di apertura effettivo dell'inventario (`Orario di apertura: HH:MM - HH:MM`) invece di lasciarlo scoprire solo in caso di errore di validazione.
        *   **Orario di arrivo:** campo testuale obbligatorio (non uno stepper — coerente con lo stile input testuali del resto del progetto), precompilato in base a `orario_apertura`/`orario_chiusura` dell'inventario e all'ora attuale se la data selezionata è oggi (`defaultOrario()`); validato (`validateOrario()`, locale al file) contro l'intervallo apertura-chiusura e contro "non nel passato se oggi" — stesso genere di controllo di `validateOrarioArrivo` in `utils/piscinaMappa.ts`, ma con in più il vincolo apertura/chiusura che qui serve perché non c'è già una prenotazione a monte da cui ereditare un orario valido. **Maschera di input:** `keyboardType="numeric"` + `formatOrarioInput(previous, next)` (helper in `utils/piscinaMappa.ts`) sull'`onChangeText` — l'utente digita solo cifre, il `:` viene inserito da solo dopo la seconda cifra (`maxLength={5}`, formato "HH:MM"). Il backspace richiede il valore precedente come argomento: se l'utente cancella proprio il `:` (le cifre digitate restano invariate rispetto a prima), l'helper rimuove anche l'ultima cifra — senza questo accorgimento il `:` si "ri-materializza" subito dopo il backspace, dando l'impressione che il tasto canc non abbia alcun effetto. Stessa maschera riusata su **tutti** gli altri campi orario testuali dell'app (stesso pattern "Es. 15:30"): `AssignPostazioneForm.tsx`/`OccupantForm.tsx` (`orarioArrivo`, mappa staff, sezione 5) ed `EditPrenotazioneSheet.tsx` (`ora`, modifica prenotazione dalla lista "Clienti del giorno", sezione 5) — nessun campo orario testuale dell'app ne è escluso.
            *   **Quadrante orologio visivo (`TimePickerModal` di `react-native-paper-dates`), solo su questo campo:** l'intera "sezione input" (non un vero `Input`/`InputField`, ma un `Pressable` stilizzato per somigliargli — stesso pattern già usato per il selettore cliente in `AssignPostazioneForm.tsx`, sezione 5) apre al tap il modale della libreria, con lancetta trascinabile e un pulsante per passare all'inserimento da tastiera, integrato. **Prima versione:** un quadrante scritto da zero (`ClockTimePicker.tsx`, stesso principio "solo primitive gluestack-ui" di `CalendarPicker.tsx`), poi sostituito su richiesta esplicita con una libreria — la scelta di partire senza dipendenze era corretta per il resto dell'app (calendario, mappa piscina), ma per un time-picker con lancette trascinabili il costo di manutenzione/robustezza cross-platform di una reimplementazione da zero non valeva il vantaggio di "zero dipendenze": **valutate `@react-native-community/datetimepicker`** (richiama i picker nativi di iOS/Android, ma sul web ricade su un semplice `<input type="time">` del browser e richiede comunque un `expo prebuild`/dev client per essere testato, niente Expo Go) **e `react-native-paper-dates`** (scelta finale: renderizzato in puro JS/RN, quindi coerente su iOS/Android/web senza rebuild nativo, e il suo `TimePicker` replica esattamente il quadrante-con-lancetta-trascinabile-più-tastiera che era stato costruito a mano).
            *   **Provider dedicato, non nel layout radice:** `react-native-paper-dates`/`react-native-paper` richiedono un `PaperProvider` (per il `Portal` del modale) e un `SafeAreaProvider` (insets usati internamente dal modale) come antenati — aggiunti in `app/cliente/_layout.tsx`, non in `app/_layout.tsx`: il secondo design system (Material Design 3, colori/tipografia diversi da gluestack-ui) resta confinato al gruppo di rotte `/cliente/*`, l'unico che ne ha bisogno. `paperTheme` sovrascrive solo `colors.primary` con lo stesso sky-600 (`#0284c7`) usato nel resto dell'Area Cliente, per non stonare troppo. `registerTranslation('it', it)` (traduzione italiana della libreria) va chiamata una sola volta a livello di modulo. Le icone del modale (`settings.icon`) usano `MaterialCommunityIcons` da `@expo/vector-icons` (già bundlato da Expo, nessun link nativo necessario) — la funzione di render è dichiarata **fuori** dal componente `ClienteLayout` (non inline nella prop), altrimenti verrebbe ricreata ad ogni render invalidando il context di `PaperProvider`.
            *   **Nessun vincolo di orario applicato nel picker stesso:** a differenza del quadrante scritto a mano (che disabilitava/nascondeva ore e minuti fuori da apertura/chiusura/"non nel passato"), `TimePickerModal` non supporta un intervallo selezionabile — l'utente può scegliere qualunque orario nel quadrante. Il controllo resta comunque effettivo: la validazione esistente al submit (`validateOrario()`, locale al file, invariata) intercetta un orario fuori apertura/chiusura o nel passato con lo stesso messaggio di errore di prima — nessuna regressione, solo un feedback posticipato al submit invece che disabilitato in tempo reale sul quadrante.
            *   **Tastiera sempre disponibile anche a orologio aperto:** un campo testuale mascherato (`formatOrarioInput`) in cima al foglio resta editabile in parallelo al quadrante — scrivono e leggono lo stesso `value`, quindi digitare aggiorna subito la lancetta/i segni evidenziati e viceversa, utile per un orario esatto non allineato ai 5 minuti del quadrante. **Due passaggi** (ore poi minuti, step di 5 minuti — stesso schema dei time picker nativi Android/iOS, bypassabile scrivendo un minuto esatto nel campo testuale): il quadrante mostra solo le ore comprese tra `minTime`/`maxTime` (props passate dal chiamante, qui `orarioBounds()` locale al file: apertura↔chiusura, con il minimo alzato all'ora attuale se si sta prenotando per oggi) — **non** un quadrante fisso 1-12: evita del tutto la necessità di un selettore mattina/pomeriggio, dato che l'orario piscina è sempre un intervallo diurno contenuto in una sola giornata. All'interno dell'ora scelta, i singoli minuti fuori da `minTime`/`maxTime` sono disabilitati (sbiaditi, non selezionabili) invece di essere nascosti — coerente con lo stile "disabled visibile" già usato altrove nell'app (es. giorni fuori mese nel calendario) invece di un elenco che cambia lunghezza in base all'ora.
        *   **Totale stimato:** calcolato lato client (quantità × prezzo per ogni risorsa, dai campi dell'inventario già caricato) e aggiornato ad ogni modifica delle quantità — puramente informativo, il backend non lo persiste. Accanto all'etichetta, un pulsante icona (`InfoIcon` di lucide, `components/ui/icon/index.tsx`+`index.web.tsx`) apre/chiude — stato locale `isInfoPrezzoOpen`, nessun `Actionsheet`/alert — una **card a scomparsa** sotto la riga del totale che avvisa come il prezzo dell'ingresso possa variare in base all'orario di arrivo e alla presenza di bambini: il totale mostrato è quindi solo una stima, non il prezzo finale. Preferita a un `Alert`/`window.alert` (usato invece altrove nel progetto, es. `ServiziClienteSection.tsx`, per messaggi che interrompono il flusso) perché qui l'informazione è consultabile ma non deve bloccare l'utente con un popup modale su un dettaglio secondario del form.
        *   **Creazione:** `createCliente()` (upsert per telefono, come da sezione 1 — solo `nome`/`telefono`, niente `note`) seguito da `createPrenotazionePiscina()` con **`stato: 'PENDING'`** e `note` valorizzata dal form (non `'CONFIRMED'` come il walk-in da mappa staff — qui il cliente non è fisicamente presente, la richiesta resta in attesa di conferma dello staff). Gli errori di validazione del backend (dict per-campo, es. `{"ombrellone": "Disponibilità... Residui: 2"}`) vengono ridotti a un messaggio unico con lo stesso pattern già usato in `PiscinaSheetsContext.tsx` (`Object.values(detail).flat().join(' ')`). Al successo, la pagina mostra una schermata di conferma dedicata invece di un semplice alert, con riepilogo, un riquadro "Scarica biglietto (PDF)" (sotto) e un pulsante **"Torna alla home"** che riporta a `/cliente` (la landing con tutti i servizi, sezione 7) — non a `/cliente/piscina` (il solo elenco piscine): dopo aver completato una prenotazione ha più senso restare un punto di partenza generico piuttosto che un elenco specifico di quel servizio.
        *   **Biglietto PDF scaricabile subito, anche da `PENDING`:** l'`id` restituito da `createPrenotazionePiscina()` viene salvato in state e usato da `getBigliettoUrl(id)` (`src/services/prenotazioni.ts`) per costruire l'URL pubblico di `GET .../scarica_biglietto/` (sezione 2 — reso `AllowAny` e disponibile anche per `PENDING`, non solo `CONFIRMED`, proprio per questo flusso). Il download vero e proprio passa da `apriBigliettoPdf(url, prenotazioneId)` (`src/utils/biglietto.ts`), cross-platform: su **web** apre semplicemente l'URL in una nuova scheda (`window.open`) e lascia che sia il browser a gestire il download grazie all'header `Content-Disposition: attachment` già impostato dal backend; su **nativo** (iOS/Android, dove non esiste un "download del browser") scarica il PDF in `Paths.cache` con `File.downloadFileAsync` (API `expo-file-system` v56, non la vecchia `FileSystem.downloadAsync`) e apre subito il foglio di condivisione di sistema con `Sharing.shareAsync` (`expo-sharing`), da cui l'utente può salvarlo o aprirlo con un'app PDF. Entrambe le librerie sono importate dinamicamente (`await import(...)`) dentro l'helper, non in cima al file, per evitare di caricare i moduli nativi quando non servono (web) — nessun impatto pratico dato che `expo-sharing` va comunque bene su web (implementazione `Web Share API`), ma tiene il percorso nativo isolato.
2.  **`src/components/cliente/BackButton.tsx`** — pulsante "Torna indietro" condiviso, usato al posto di markup ripetuto in tutte e quattro le occorrenze in `app/cliente/` (landing, elenco piscine, e le due nello schermo di prenotazione — stato d'errore e form principale): `variant="outline"`, `border-2 border-sky-300 bg-white shadow-sm` + `ArrowLeftIcon`, `onPress` di default `router.back()` (sovrascrivibile via prop, così lo stato d'errore della pagina di prenotazione — che non ha uno stack di navigazione garantito — può passare un fallback esplicito se in futuro serve). Prima di questo accorpamento, due delle quattro occorrenze erano rimaste indietro come semplice `Button variant="link"`/testo sottolineato invece del bottone bordato con icona già introdotto altrove nella stessa sezione — un'inconsistenza visiva tra pagine della stessa area, ora impossibile perché c'è un solo componente da aggiornare.
3.  **`src/components/cliente/ServiziClienteSection.tsx`** — griglia di card servizio, stesso pattern a due stati di `TIPO_INVENTARIO_OPTIONS` (sezione 5). La card **Piscina** non è più sempre attiva: al mount la sezione chiama `listPiscinaInventari()` e calcola `isPiscinaDisponibile = items.some(i => i.isActive)`; se **nessun** inventario piscina ha un listino attivo, la card si disabilita da sola con badge **"Non disponibile"** (grigio/slate, per distinguerla visivamente da "Presto") e il tap mostra un alert dedicato invece di navigare a `/cliente/piscina`. **Ristorante** e **Asporto** restano sempre `disponibile: false` con badge "Presto" (ambra) — non esistono ancora modelli/API backend per questi due (stesso stato dei tipi inventario "Asporto"/"Sala" lato staff). Il tap su una card non disponibile mostra un alert "Prossimamente" (`Platform.OS === 'web' ? window.alert : Alert.alert`, stesso pattern usato in `PiscinaInventarioSection.tsx` per non ripetere `Alert.alert` che è un no-op su react-native-web).
4.  **`src/components/cliente/ClienteFooter.tsx`** — footer condiviso in fondo a ogni schermata di `app/cliente/` (montato sia in `app/cliente/index.tsx` sia in `app/cliente/piscina.tsx`, sotto il pulsante "Torna indietro"), reso come una banda scura distinta dal resto della pagina (che resta chiara, tema forzato light, sezione 4), pattern comune di molti siti (footer scuro su body chiaro). **Colore abbinato al resto dell'app, non un grigio/blu generico:** lo sfondo `--background` dell'app è `rgb(234 224 200)` / `#eae0c8` (crema caldo, tonalità ~42° in HSL, sezione 4); il footer usa `bg-[#332a18]`, una tonalità scura calcolata sulla stessa tonalità/saturazione ma a luminosità molto più bassa — non un Tailwind `slate`/`stone` generico (che ha una dominante fredda blu-grigia e stonerebbe con il crema) — e i testi/icone usano tinte `amber-*` (`text-amber-50/100/200`) invece di `slate-*` per restare nella stessa famiglia calda. **Nessun angolo arrotondato** (non più `rounded-3xl`/`rounded-t-3xl`): il rettangolo è netto sui quattro lati. **A tutta larghezza (full-bleed):** entrambe le pagine ospitanti usano lo stesso `contentContainerClassName="px-4 py-6 md:px-8 md:py-10"` sullo `ScrollView`; il footer annulla quel padding sui lati e in basso con margini negativi corrispondenti (`-mx-4 -mb-6 md:-mx-8 md:-mb-10`), così lo sfondo scuro tocca i bordi orizzontali dello schermo e il fondo dell'area scrollabile — se in futuro il padding del contenitore cambia in una delle due pagine va aggiornato anche qui.
    *   **Struttura a tre colonne (responsive):** il contenuto è diviso in tre blocchi — brand (logo + "Le Gole" + una riga descrittiva), "Contatti" (telefono/email) e "Social" (i tre badge) — ciascuno con un'etichetta `FooterColumnLabel` (maiuscolo, `tracking-widest`, `text-amber-200/50`). Su mobile restano impilati in verticale (comportamento base di `VStack`); da `md:` in su lo stesso `VStack` passa a `md:flex-row md:items-start md:justify-between` (stesso trucco già usato in `ServiziClienteSection.tsx`, sezione 7, per passare da colonna a riga), affiancando i tre blocchi invece di lasciare grandi spazi vuoti su schermi larghi. Un divisore (`Box h-px bg-white/10`) separa questa riga dalla riga di copyright finale.
    *   **`src/components/cliente/SocialIcons.tsx`** — `InstagramIcon`/`FacebookIcon`/`WhatsAppIcon`, disegnate a mano con primitive `react-native-svg` (`Svg`/`Path`/`Rect`/`Circle`) invece che con una libreria di icone brand: nel progetto non è installato né `lucide-react-native` né `@expo/vector-icons` (il set in `components/ui/icon/index.tsx` è generato dalla CLI gluestack-ui/lucide ma copre solo icone UI generiche, non i loghi social) — introdurre una dipendenza solo per tre loghi non era giustificato.
    *   **Attenzione:** i valori in `CONTATTI`/`SOCIAL_LINKS` (in `ClienteFooter.tsx`) sono segnaposto (telefono, email ed URL social non reali, marcati con un commento `TODO` nel file) — vanno sostituiti con i recapiti e i profili social effettivi de Le Gole prima del rilascio, stesso discorso già valido per `DEFAULT_FROM_EMAIL` lato backend (sezione 1). Un link diretto a Google Maps con l'indirizzo era stato provato in questa colonna e poi rimosso (esperienza d'uso non soddisfacente) — se reintrodotto in futuro, va aggiunta una nuova icona `MapPinIcon` in **entrambi** i file di `components/ui/icon/` (vedi nota sotto), non solo in `index.tsx`.
    *   **Icone: due file paralleli, non uno solo.** `components/ui/icon/` contiene sia `index.tsx` (nativo iOS/Android, usa `Path`/`Svg` da `react-native-svg`) sia `index.web.tsx` (web, usa tag SVG intrinseci minuscoli `<path>`/`<svg>` — nessun import da `react-native-svg`): Metro risolve automaticamente `index.web.tsx` quando la piattaforma target è `web`, ignorando `index.tsx`. Aggiungere una nuova icona richiede la stessa definizione in **entrambi** i file — dimenticare `index.web.tsx` non genera alcun errore di build/TypeScript (l'icona esiste comunque nell'altro file, l'import risolve), ma produce silenziosamente un `<svg>` vuoto senza `<path>` figli su web, cioè un'icona invisibile pur senza eccezioni in console (riscontrato in prima persona aggiungendo e poi rimuovendo `MapPinIcon`).
5.  **Attenzione — nuovo gruppo di rotte richiede il riavvio del dev server Metro:** aggiungere una cartella di route del tutto nuova (qui `app/cliente/`, con relativo `_layout.tsx`) mentre il dev server è già in esecuzione **non** viene rilevato dal file watcher di Metro allo stesso modo di una modifica a un file esistente — bundle e tipi generati (`.expo/types/router.d.ts`) restano quelli di prima, e la navigazione verso la nuova rotta fallisce con la schermata "Unmatched Route"/pagina non trovata finché il dev server non viene fermato e riavviato (`npm run web` / `expo start`). Va fatto ogni volta che si introduce un **nuovo segmento di primo livello** sotto `app/` (non serve per nuovi file dentro una cartella già esistente e già servita). Una volta riavviato, `_layout.tsx` rende `/cliente` una rotta "nuda" valida (stesso comportamento di `/staff`) — **non** esiste invece `/cliente/index` come rotta distinta una volta che il gruppo ha un `_layout.tsx` (stesso discorso per `/staff/index`, mai usato nel codice): usare sempre `/cliente` in `router.push`.

---

## 8. Prossimi Passi (Fase 2)
1.  ~~Configurazione Frontend: Setup di Axios su React Native per la gestione delle chiamate API e l'iniezione automatica del token JWT (Bearer).~~ **Completato** (vedi sezione 3).
2.  ~~UI Kit: integrazione gluestack-ui v5 + NativeWind, tema light forzato, schermate base (selezione ruolo, login staff, home staff).~~ **Completato** (vedi sezione 4).
3.  ~~Sviluppo UI Piscina: card inventario ristrutturate + mappa spaziale zoomabile per posizionare ombrelloni/gazebi e assegnare clienti/lettini/sdraie per data.~~ **Completato** (vedi sezione 5).
4.  ~~Gestione Staff su invito (creazione senza password, attivazione/reset password via email) lato superuser.~~ **Completato** (vedi sezione 6).
5.  ~~Area Cliente: landing con descrizione app + card servizi cliccabili.~~ **Completato** (vedi sezione 7).
6.  ~~Flusso di prenotazione piscina lato cliente: dati cliente, scelta data/orario, disponibilità residua, scelta risorse e creazione `Prenotazione_Piscina` (stato `PENDING`, da confermare lato staff).~~ **Completato** (vedi sezione 7, `app/cliente/piscina/[inventarioId].tsx`). ~~Notifica allo staff quando arriva una nuova richiesta `PENDING`~~ **Completato** (vedi sezione 11). Resta da fare: un modo per il cliente di consultare lo stato della propria richiesta dopo l'invio (oggi riceve solo la conferma a schermo, nessuna area "le mie prenotazioni").
7.  **Estensione Backend Ristorante:** Creazione dei modelli per `Sala`, `Tavolo`, `Prenotazione_Tavolo` e `Prenotazione_Asporto`.
8.  **Estensione Backend Menu:** Creazione del catalogo prodotti.
9.  ~~Calendario per la selezione data (mappa staff e prenotazione cliente), con conteggio prenotazioni per giorno lato staff.~~ **Completato** (vedi sezione 5, `CalendarPicker`). Restano da fare: pinch-to-zoom nativo (`react-native-gesture-handler`) e inserimento nuove prenotazioni direttamente dalla pagina mappa.
10. **Email in produzione:** `DEFAULT_FROM_EMAIL` usa ancora l'indirizzo di test di Resend (`delivered@resend.dev`) — va sostituito con un mittente sul dominio verificato prima del rilascio.

---

## 9. Testing

### Backend (`pytest` + `pytest-django` + `factory_boy` + `pytest-cov`)
*   **Scelta e motivazione:** stack scelto al posto del semplice `django.test.TestCase`/DRF `APITestCase` per la mole di regole di validazione e casi limite documentati in questa guida (anti-overbooking, soglie orario ridotto pomeridiano, soft-delete condizionale di `Postazione`, sola lettura sui giorni passati, upsert per telefono/email, ecc., sezioni 1-2-5) — le fixture e il `parametrize` di `pytest` riducono il boilerplate ripetuto rispetto a un `setUp()` per ogni variante dello stesso validatore. `factory_boy` è pensato per generare rapidamente istanze valide dei modelli di dominio (`Cliente`, `PiscinaInventario`, `Postazione`, `PrenotazionePiscina`, ecc.) senza ripetere a mano gli stessi campi obbligatori in ogni test. `pytest-cov` mostra a colpo d'occhio quali file/righe non sono ancora coperti, utile soprattutto per i validator con più rami (es. `PrenotazionePiscinaSerializer.validate()`, sezione 2).
*   **Configurazione:** `le-gole-be/pytest.ini` (`DJANGO_SETTINGS_MODULE = backend.settings`, `python_files = test_*.py`, `testpaths = users struttura prenotazioni` — `menu` è escluso perché l'app non è ancora attiva in `INSTALLED_APPS`, sezione 1, va aggiunta a `testpaths` quando verrà sviluppata; `addopts` abilita sempre `--cov=users --cov=struttura --cov=prenotazioni --cov-report=term-missing`, quindi ogni `pytest` stampa già il riepilogo di copertura senza flag aggiuntivi). `le-gole-be/.coveragerc` esclude dalla copertura ciò che non è logica applicativa: `*/migrations/*`, `*/test/*` (il codice di test stesso), `manage.py`, `backend/*` (settings/urls/wsgi), `*/apps.py`, `*/admin.py`. Il DB di test usa le stesse credenziali Postgres di `.env` (`DB_*`): pytest-django crea/distrugge un database `test_<DB_NAME>` ad ogni run (nessun `--reuse-db` di default, verificato che la creazione funzioni con le credenziali locali).
*   **Struttura per-app, mai un solo file:** ogni app ha un pacchetto `test/` (non un singolo `tests.py` monolitico, lo stub di default generato da Django) — `users/test/`, `struttura/test/`, `prenotazioni/test/`, ciascuno con un `__init__.py` e più moduli `test_*.py` divisi per area (es. modelli, serializer, singola viewset/azione custom). Data la quantità di regole di business specifiche documentate in questa guida, un unico file per app diventerebbe rapidamente troppo lungo da navigare — ogni nuovo gruppo di test va in un modulo dedicato, non accodato a un file esistente che copre un argomento diverso.
*   **Esecuzione:** `pytest` (tutte le app, con report di copertura) o `pytest <app>` (es. `pytest users`) dalla root `le-gole-be/`, con il venv attivo.
*   **App `users` (prima suite scritta, 45 test — usata come riferimento per lo stile delle prossime):** `users/test/factories.py` (`UtenteFactory`/`SuperUserFactory`/`ClienteFactory`, `factory_boy`, con `skip_postgeneration_save = True` sulla password per evitare il `DeprecationWarning` di una doppia `save()`), `users/test/conftest.py` (fixture condivise: `api_client`, `staff_user`/`superuser`/`cliente`, `staff_client`/`superuser_client` già autenticati via `force_authenticate`), poi `test_models.py`, `test_permissions.py` (`IsSuperUser`), `test_serializers.py` (validazione email duplicata case-insensitive, `is_superuser` read-only, robustezza password — testata su **entrambi** `ActivateAccountSerializer` e `PasswordResetConfirmSerializer`, stesso `validate_password` duplicato tra le due classi senza ereditarietà condivisa: la copertura ripetuta previene una divergenza silenziosa se in futuro una delle due venisse aggiornata e l'altra dimenticata), `test_views_staff.py` (permessi `IsSuperUser` su `UtenteViewSet` — inclusi i due status code diversi per un accesso negato: **401** per richiesta anonima, **403** per un utente autenticato ma senza i permessi, comportamento di default di DRF quando l'autenticatore ha avuto successo ma il permesso fallisce comunque —, flussi `activate`/`reset-password/*` con sia uid sia token non validi, guardrail anti-lockout di `set_active`), `test_views_cliente.py` (`create` pubblico con upsert per telefono, ricerca `?search=` su nome/telefono). Copertura: `users/` al 100%.
*   **App `struttura` (36 test): `struttura/test/factories.py`** (`PiscinaInventarioFactory`, `PostazioneFactory`, `PostazionePosizioneStoricoFactory` — quest'ultima importa il modello da `prenotazioni`, stesso "import verticale" già usato da `struttura/views.py`, sezione 1), **`conftest.py`** (riusa `UtenteFactory` da `users.test.factories` invece di duplicarla — le fixture di un'app possono importare le factory di un'altra, non solo i modelli/utility di produzione), poi `test_models.py`, `test_serializers.py` (range 0-100 di `pos_x`/`pos_y`, unicità condizionale di `numero` per `(inventario, deleted_at IS NULL)`, incluso il riuso del numero dopo soft-delete), `test_views_inventario.py` (permessi `IsAuthenticatedOrReadOnly`, azione `attivo`, blocco eliminazione con prenotazioni future/odierne e pulizia automatica di quelle passate), `test_views_postazione.py` (soft delete: la riga resta nel DB ma sparisce da list/retrieve/filtri, il numero liberato è riassegnabile), `test_views_postazione_storico.py` — il file più delicato, isolato apposta dagli altri viewset test per la sua complessità: verifica sia la scrittura (`registra_posizione_storico`, upsert sulla riga di oggi per più drag nello stesso giorno) sia la ricostruzione "mappa come era" per `?data=<passata>` (posizione storica corretta, esclusione delle postazioni create dopo quella data, inclusione di quelle eliminate dopo). Per backdatare `created_at`/`deleted_at` (entrambi gestiti da Django in scrittura, non impostabili passandoli a `.create()`/`.save()`) i test usano `Postazione.objects.filter(pk=...).update(...)`, un `UPDATE` SQL diretto che bypassa `auto_now_add`/`auto_now`. Copertura: `struttura/` al 100% tranne `views.py` (97%, il ramo `except ProtectedError` di `destroy()` — rete di sicurezza difensiva per vincoli `PROTECT` residui non altrimenti raggiungibile nei test, dato che il controllo esplicito sopra lo previene già in ogni scenario testabile).
*   **App `prenotazioni` (59 test — l'app più corposa, essendo quella con la maggior parte della logica di business): `prenotazioni/test/factories.py`** (`PrenotazionePiscinaFactory`, `OccupazionePostazioneFactory`, `GiornoPienoPiscinaFactory`, tutte con `SubFactory` verso le factory di `struttura`/`users` invece di ricrearle localmente), poi:
    *   `test_utils.py` — `calcola_disponibilita()` isolata dalla view (residuo pieno senza prenotazioni, somma di più prenotazioni sulla stessa data/inventario, esclusione `CANCELLED`, esclusione tramite `exclude_id` in un update, isolamento per data/inventario).
    *   `test_views_prenotazione_create.py` — tutti i rami di `PrenotazionePiscinaSerializer.validate()` raggiungibili in creazione: orario fuori apertura/chiusura, soglia ridotto pomeridiano in **entrambe le direzioni** (ridotti prima della soglia rifiutati, interi dopo la soglia rifiutati solo se il ridotto è configurato — parametrizzato sul prezzo), anti-overbooking su tutte e quattro le risorse (parametrizzato con `pytest.mark.parametrize`, un solo test invece di quattro quasi identici), blocco "giorno pieno" per il self-service anonimo e bypass per lo staff autenticato.
    *   `test_views_prenotazione_update.py` — separato dal file di creazione perché copre un comportamento specifico dell'update: un PATCH parziale che tocca un solo campo non deve richiedere gli altri (eredita `data`/`ora`/i contatori dall'istanza), e l'anti-overbooking su un PATCH esclude correttamente la prenotazione corrente dal proprio stesso conteggio residuo (altrimenti una prenotazione già al limite non sarebbe mai più modificabile, nemmeno su un campo non correlato).
    *   `test_views_prenotazione_crud.py` — permessi (`IsAuthenticated` su tutto tranne `create`), filtri (`data`/`stato`/`cliente_id`), e la `destroy()` che elimina anche le `OccupazionePostazione` collegate (non solo `SET_NULL`, altrimenti la postazione risulterebbe ancora "occupata" sulla mappa dopo l'eliminazione, sezione 5).
    *   `test_views_prenotazione_actions.py` — le tre azioni custom pubbliche/miste: `disponibilita` (pubblica, residui + flag `pieno`), `scarica_biglietto` (pubblica, genera davvero il PDF con WeasyPrint per `PENDING`/`CONFIRMED`, bloccata per `CANCELLED`), `conteggi` (riservata allo staff, aggregazione per giorno in un mese, esclude `CANCELLED`).
    *   `test_views_occupazione_postazione.py` / `test_views_giorno_pieno.py` — CRUD + filtri della prima, CRUD + azione pubblica `calendario` della seconda.
    *   `test_models.py` — `__str__` dei quattro modelli non astratti (aggiunto per coerenza con lo stile delle altre due app, non nella prima stesura).
    *   Copertura: `prenotazioni/` al 100%.
*   **Copertura complessiva del backend dopo le tre suite: 99%** (`639` statement, `2` non coperti — solo il ramo difensivo di `struttura/views.py` sopra). Nota di processo: il numero di `pytest-cov` ha guidato due correzioni concrete durante la scrittura (non solo una metrica finale) — ha fatto emergere sia la mancanza del test su `reset_password_confirm` con uid non decodificabile (sezione precedente) sia la necessità di un `test_models.py` per `prenotazioni` (sopra), entrambi aggiunti prima di considerare le rispettive suite complete.
*   **Due gotcha emersi scrivendo questa suite, validi per ogni test futuro che usa `APIClient`:**
    1.  **Email in test:** `EMAIL_BACKEND` di produzione è `anymail.backends.resend.EmailBackend` (sezione 1) — inviare davvero un'email ad ogni test colpirebbe l'API Resend reale. Le azioni che inviano email (`create` di `UtenteViewSet`, `reset_password_request`) vanno testate con una fixture `autouse` che sovrascrive `settings.EMAIL_BACKEND` su `django.core.mail.backends.locmem.EmailBackend` (via la fixture `settings` di pytest-django, che la ripristina automaticamente a fine test) e verifica il contenuto tramite `django.core.mail.outbox`.
    2.  **`APIClient.post()`/`.patch()` con un body che contiene un booleano:** senza `format="json"` esplicito, DRF's test client codifica il payload come `multipart/form-data` di default — un `False` Python diventa la stringa `"False"` lato server. Per un endpoint come `set_active` che fa `isinstance(is_active, bool)` (sezione 1), questo produce un `400` inatteso invece del `200` di successo. **Va sempre passato `format="json"`** su ogni chiamata `APIClient` con un body, non solo su quelle con booleani (coerenza, ed evita la stessa sorpresa se un campo booleano viene aggiunto in futuro a un payload che oggi ne è privo).

---

## 10. Docker (Backend + Postgres)

Containerizzazione del solo backend Django + Postgres, pensata per lo sviluppo locale (non per un deploy di produzione) — il frontend Expo/React Native non è containerizzato: Metro va comunque eseguito sull'host (o in un dev client) per restare raggiungibile da device/emulatore, e non trarrebbe beneficio da Docker allo stadio attuale del progetto.

*   **`le-gole-be/Dockerfile`:** immagine `python:3.13-slim` (stessa versione Python di sezione 0). Installa via `apt` solo `libpango-1.0-0`, `libpangoft2-1.0-0`, `fontconfig`, `fonts-liberation` — le librerie di sistema richieste da **WeasyPrint** per generare il PDF del biglietto piscina (`scarica_biglietto`, sezione 2): i pacchetti Python di `requirements.txt` da soli non bastano, WeasyPrint si appoggia a Pango per il layout/shaping del testo. **Niente `libgdk-pixbuf`**: le versioni recenti di WeasyPrint (qui 69.0) non dipendono più da cairo/gdk-pixbuf, e su Debian *trixie* (base di `python:3.13-slim` al momento di questo Dockerfile) il pacchetto si chiama comunque diversamente (`libgdk-pixbuf2.0-0` non esiste più con questo nome — build fallita e corretta durante la stesura di questa sezione). `requirements.txt` è copiato e installato in un layer separato dal resto del codice per sfruttare la cache di Docker (le dipendenze cambiano molto meno spesso del codice). `CMD` applica le migrazioni e avvia **gunicorn** (`backend.wsgi:application`, `--workers ${GUNICORN_WORKERS:-3}` — override passando la variabile d'ambiente al servizio `backend` se serve alzare il numero di worker, default adeguato per il traffico contenuto di questa fase di test web) — non più `runserver` (single-threaded, che Django stesso sconsiglia esplicitamente in produzione). **Nessun `--reload`**: un cambiamento al codice (bind mount, sotto) richiede un riavvio esplicito del container (`docker compose restart backend`), coerente con un ambiente che si comporta come un vero deploy. **Nota:** i file statici (solo l'admin Django li usa in questo progetto, essendo il resto un'API DRF pura) non sono serviti da gunicorn senza configurazione aggiuntiva (whitenoise o un reverse proxy) — non ancora sistemato, l'admin risulterebbe privo di stile se usato dietro questo setup; non impatta l'API stessa.
*   **`docker-compose.yml`** (root del repo): due servizi.
    *   **`db`** — `postgres:17` (stessa versione di sezione 0), con un volume nominato (`postgres_data`) per persistere i dati tra un `docker compose down` e il successivo `up`, e un `healthcheck` (`pg_isready`) da cui dipende l'avvio del backend (`depends_on: condition: service_healthy` — richiede Docker Compose V2, il comando `docker compose`, non il vecchio eseguibile standalone `docker-compose`).
    *   **`backend`** — build da `./le-gole-be`, bind mount `./le-gole-be:/app` per lo sviluppo (il codice sull'host è la fonte di verità, le modifiche si riflettono subito senza ricostruire l'immagine).
    *   **Porta di `db` mappata su 5433, non 5432:** in questo progetto Postgres gira normalmente **anche in locale fuori da Docker** (sezione 1, `DB_HOST=localhost` in `.env`) — mappare `5432:5432` fallirebbe (o entrerebbe in conflitto) se quell'istanza nativa è già in ascolto, come verificato durante la stesura di questa sezione. Il backend nel container comunica comunque con `db` sulla porta interna 5432 (invariata); 5433 serve solo per ispezionare il Postgres containerizzato da un client sull'host, se mai necessario.
*   **Due database realmente distinti, non solo due credenziali per lo stesso — `.env` + `.env.prod`:**
    *   **`le-gole-be/.env`** — database di **sviluppo/test**: il Postgres **nativo** in locale (`DB_HOST=localhost`), lo stesso ispezionabile con pgAdmin e usato da `pytest` per il DB di test effimero (sezione 9). Contiene anche le variabili non legate al DB (`SECRET_KEY`, `DEBUG`, `FRONTEND_*`, `RESEND_API_KEY`).
    *   **`le-gole-be/.env.prod`** (nuovo) — database di **produzione**: il Postgres **containerizzato** dal servizio `db` di `docker-compose.yml`, con un proprio nome database (`le_gole_prod_db`, non `le_gole_db`) e un proprio utente applicativo dedicato (`le_gole_app`, non il superuser `postgres` usato in locale) — così sono davvero due database separati (dati, credenziali, persino il nome utente), non la stessa istanza con due password diverse. Contiene sia `DB_NAME`/`DB_USER`/`DB_PASSWORD`/`DB_HOST=db` (letti da Django) sia `POSTGRES_DB`/`POSTGRES_USER`/`POSTGRES_PASSWORD` (stessi valori, nomi richiesti dall'immagine ufficiale di Postgres per inizializzare il database al primo avvio) — vanno tenuti manualmente allineati se cambi le credenziali.
    *   **Nel `docker-compose.yml`:** il servizio `db` carica solo `.env.prod`. Il servizio `backend` carica **entrambi** i file, in quest'ordine: `.env` prima (base: `SECRET_KEY` e le altre variabili condivise), poi `.env.prod` (sovrascrive le sole chiavi `DB_*`/`POSTGRES_*` con le credenziali del database containerizzato) — `env_file:` di Compose applica i file in ordine, l'ultimo vince per le chiavi in comune. Senza questo secondo file il backend erediterebbe `DB_HOST=localhost`/`DB_NAME=le_gole_db` da `.env`, che dentro il container non è né raggiungibile né il database corretto.
    *   **`le-gole-be/.env.example`** e **`.env.prod.example`** (entrambi tracciati in git, a differenza di `.env`/`.env.prod` — `.gitignore` esclude `.env.*` con due eccezioni esplicite `!.env.example`/`!.env.prod.example`): documentano tutte le variabili necessarie per ciascuno dei due database, senza segreti reali.
*   **`le-gole-be/.dockerignore`:** esclude `.venv/` (ambiente virtuale Windows dell'host, inutile e pesante dentro un'immagine Linux — i pacchetti Python vengono comunque reinstallati nell'immagine da `requirements.txt`, senza alcuna dipendenza dalla venv dell'host), cache/artefatti di test (`.pytest_cache/`, `.coverage`, `htmlcov/`), e `.env`/`.env.prod` (le credenziali arrivano solo tramite `env_file:` a runtime, mai copiate dentro un layer dell'immagine).
*   **Gotcha reale, scoperto testando la configurazione — un `$` in `.env` può essere "mangiato" da Docker Compose:** `env_file:` in Compose interpola `${VAR}`/`$VAR` proprio come farebbe con `environment:` — se un valore contiene un `$` seguito da caratteri che sembrano un nome di variabile (es. una `SECRET_KEY` generata casualmente che conteneva `$ll`), Compose lo sostituisce silenziosamente con una stringa vuota e stampa solo un warning ("The \"ll\" variable is not set"), **senza mai fallire** — il valore che arriva al container è quindi diverso (e più corto) di quello che `django-environ` legge nell'esecuzione diretta (che invece non fa alcuna interpolazione, `$` è sempre letto alla lettera). Non esiste un modo di escape che dia lo stesso identico valore a entrambi i lettori (django-environ non collassa `$$` in `$`, quindi raddoppiare il simbolo lo cambierebbe comunque per l'uso diretto). Soluzione adottata: **evitare del tutto il carattere `$` nei valori di `.env`** — la `SECRET_KEY` è stata rigenerata con `secrets.token_urlsafe()` (solo lettere/cifre/`-`/`_`) proprio per eliminare la classe di problema, non solo l'istanza specifica incontrata. Verificare con `docker compose config` (mostra i valori risolti) prima di assumere che un `.env` esistente sia Compose-safe.
*   **Verificato end-to-end** (build, `up`, migrazioni applicate automaticamente sul database `le_gole_prod_db`/utente `le_gole_app` — confermato con `psql \l` dentro il container che è realmente un database/utente distinto da quello locale —, persistenza dati nel volume tra un riavvio del container e l'altro, generazione reale del PDF biglietto tramite `scarica_biglietto` con le sole librerie di sistema elencate sopra, e che la suite `pytest` locale continui a funzionare invariata contro il Postgres nativo dopo la separazione) prima di considerare la configurazione completa.
*   **Nota:** il container esegue ora un vero server WSGI (gunicorn, vedi sopra), ma resta comunque da completare l'hardening per un deploy reale (`DEBUG=False`, `ALLOWED_HOSTS` esplicito, mittente email reale — sezione 8, già tracciati come TODO) prima di un rilascio effettivo.

---

## 11. Notifiche In-App Staff per Nuove Prenotazioni

Obiettivo del progetto in questa fase: rilascio solo web per testare le funzionalità, prima dell'implementazione finale e del rilascio su App Store/Play Store — le scelte tecniche sotto (polling invece di push reali) sono dimensionate su questo orizzonte, non sulla versione definitiva.

*   **Meccanismo — polling, non websocket/push:** il backend è WSGI (`runserver`/gunicorn futuro, sezione 10), senza alcuna infrastruttura realtime (niente Django Channels). Notifiche push reali (`expo-notifications`, token per device salvato lato backend, invio alla creazione della prenotazione) sono state scartate per questa fase: lavoro sostanzialmente più grande, e non necessario finché il target è "solo web" — da rivalutare quando si passerà alle build native per gli store. Il frontend interroga `GET /v1/prenotazioni/piscina/?stato=PENDING` (nuova funzione `listPrenotazioniPendenti()`, `src/services/prenotazioni.ts` — riusa il `filterset_fields` già esistente lato backend, **nessuna modifica al backend richiesta**) ogni 20s (`POLL_INTERVAL_MS`) mentre lo staff è autenticato.
*   **`src/context/StaffNotificationsContext.tsx`** (nuovo) — montato dentro `app/staff/_layout.tsx` (non nel root layout, sezione 3: la sessione staff è già gestita lì da `AuthProvider`, e il polling non deve mai partire sulle rotte pubbliche `/cliente/*`), avvolge l'intero contenuto autenticato (`<Slot />` incluso).
    *   **Conteggio badge = "nuove dall'ultimo controllo", non il totale pendenti:** distinzione esplicita richiesta per evitare di confondere "quante richieste attendono un'azione" (informazione già disponibile aprendo il pannello) con "quante sono arrivate senza che tu le vedessi" (il vero senso di un badge di notifica). `lastSeenAt` (timestamp ISO) è persistito tramite `getNotificheLastSeenAt`/`saveNotificheLastSeenAt` (nuove funzioni in `src/utils/storage.ts`, stesso wrapper web/nativo già usato per i token JWT — `localStorage` su web, `expo-secure-store` altrove) e aggiornato a "adesso" quando lo staff apre il pannello (`markAllAsSeen()`), **non** ad ogni poll — altrimenti il badge si azzererebbe da solo prima che lo staff se ne accorga. Il conteggio è quindi `pendenti.filter(p => p.created_at > lastSeenAt).length`. Persistito (non solo in memoria) apposta per non far ricomparire come "nuove" le stesse richieste dopo un refresh della pagina — frequente su web, il target attuale di questa fase.
    *   **Banner "a comparsa" per un arrivo genuinamente nuovo durante la sessione:** indipendente dal badge sopra. Un `Set` di id noti (`knownIdsRef`, in memoria, non persistito) parte `null` finché non arriva il primo poll riuscito — a quel punto contiene tutti gli id già visti, e il banner scatta **solo** per un id che compare in un poll successivo e non era nel set precedente. Questo evita di mostrare un banner per l'intero arretrato di prenotazioni già pendenti al primo caricamento della pagina (che sarebbe rumore, non una notizia), mostrandolo solo per un vero arrivo "in diretta" durante la sessione — la richiesta esplicita di essere avvisati "nel momento in cui" succede. Auto-si-chiude dopo 6s (`BANNER_DURATION_MS`) o al tap.
    *   **Conferma diretta dal pannello:** `confirmPrenotazione(id)` chiama `updatePrenotazionePiscina(id, { stato: 'CONFIRMED' })` (stesso endpoint già usato da `PiscinaSheetsContext.confirmPrenotazione` per la mappa piscina, sezione 5) e rimuove otticamente l'elemento da `pendenti` al successo. **Non** riusa `PiscinaSheetsContext`/`PiscinaMappaDataContext` direttamente: quei context sono scoperti per un singolo `inventarioId`/data selezionata sulla mappa, mentre questo pannello mostra prenotazioni **cross-piscina e cross-data** (qualunque prenotazione self-service in attesa, non solo quelle del giorno/piscina aperti sulla mappa in quel momento) — stesso endpoint, chiamata indipendente. Nota: confermare da qui non aggiorna lo stato locale della mappa se lo staff la ha aperta in un'altra scheda/tab — nessun meccanismo di cache condivisa esiste oggi nel progetto, stessa limitazione implicita già presente altrove.
*   **`src/components/staff/NotificationsBell.tsx`** (nuovo) — tre export:
    *   `NotificationsBell` — icona campanella (`BellIcon`, già presente in `components/ui/icon/index.tsx`/`index.web.tsx`, nessuna nuova libreria di icone) nell'header staff (`app/staff/_layout.tsx`, accanto all'icona di ricerca 🔍 esistente, stesso `HStack`), con un badge numerico in overlay (stesso pattern "a goccia" con anello `border-background` già usato per i conteggi del `CalendarPicker`, sezione 5) colorato `bg-amber-100`/`text-amber-700` — stessa famiglia cromatica già associata allo stato `PENDING` (`STATO_PRENOTAZIONE_BADGE`, `utils/piscinaMappa.ts`), non un rosso/rosa da "allarme": è un'attesa di conferma, non un errore. Il tap apre il pannello **e** chiama `markAllAsSeen()` nello stesso gesto.
    *   Il pannello è un `Actionsheet` (nessun componente `Toast`/`Badge` installato nel progetto, sezione 4 — stesso approccio "primitive gluestack-ui" già usato altrove) che elenca **tutte** le prenotazioni pendenti (non solo quelle "nuove": una volta viste restano comunque da confermare), più recenti prima, ciascuna con nome/telefono cliente, badge data+ora, note se presenti, riepilogo risorse (`formatIngressiSummary` + emoji, riuso diretto degli helper di `utils/piscinaMappa.ts`) e pulsante ✅ "Conferma".
    *   `NotificationsBanner` — montato come riga a comparsa **tra** l'header e il contenuto della pagina (`app/staff/_layout.tsx`, prima di `<Slot />`), non in overlay assoluto: più semplice e senza le complicazioni di z-index/positioning tra web e nativo, a costo di spingere leggermente in basso il contenuto sottostante finché il banner è visibile.
*   **Verificato end-to-end in un browser reale** (Playwright headless, dev server Expo web + backend Django contro il DB di sviluppo con dati pendenti già esistenti): campanella con badge "4" nell'header, apertura del pannello con le 4 prenotazioni in attesa correttamente ordinate ed etichettate, tap su "Conferma" che rimuove la riga e — confermato interrogando il DB direttamente — aggiorna realmente `stato` a `CONFIRMED` lato backend, nessun errore in console.

---

## 12. Deploy Backend su Render + Neon (Piano Gratuito)

Primo hosting realmente raggiungibile dal web per la fase di test (sezione 11 — "rilascio solo web per testare le funzionalità"), scelto tra le opzioni gratuite: **Render** (web service, deploy diretto da `Dockerfile`) per il backend Django + **Neon** (Postgres gestito) per il database, invece del Postgres gratuito *di Render* stesso — quest'ultimo si elimina automaticamente dopo 90 giorni sul piano free, un limite che Neon non ha. Il frontend Expo/React Native non è coperto da questa sezione: resta da scegliere dove ospitare la build web quando sarà pronta (vedi nota su `FRONTEND_ACTIVATION_URL`/`FRONTEND_RESET_URL` sotto).

*   **`render.yaml`** (root del repo) — Blueprint Render, importabile dalla dashboard puntando a questo repo. Un solo servizio `le-gole-backend` (`type: web`, `runtime: docker`, `plan: free`), con `dockerfilePath`/`dockerContext` che puntano a `./le-gole-be` (il Dockerfile non è alla radice del repo, a differenza del path di default che Render si aspetterebbe) — stesso pattern relativo già usato in `docker-compose.yml` (sezione 10). `healthCheckPath` punta a un endpoint reale già pubblico e leggero (`/api/v1/struttura/inventario-piscina/`, `IsAuthenticatedOrReadOnly`) invece di un path inventato, per far sì che Render verifichi davvero che Django (non solo il processo gunicorn) risponda prima di considerare il deploy sano.
*   **Variabili d'ambiente del Blueprint — tre trattamenti diversi:**
    *   `generateValue: true` per `SECRET_KEY` — Render genera da sé un valore casuale sicuro al primo deploy, l'utente non deve inventarne uno.
    *   `value: ...` per config non segreta, nota in anticipo e sicura da avere nel file committato: `DEBUG=False`, `ALLOWED_HOSTS=.onrender.com` (host jolly: **qualunque** sottodominio `*.onrender.com` combacia con il pattern "punto iniziale" supportato da Django — da restringere all'host esatto, o a un dominio custom, una volta usciti dalla sola fase di test), `DB_PORT`/`DB_SSLMODE`/`DB_CONN_HEALTH_CHECKS` (valori fissi richiesti da Neon, sotto), `GUNICORN_WORKERS=2` (il piano free ha poca RAM — meno dei 3 worker di default usati in locale/Docker, sezione 10, per non rischiare un OOM).
    *   `sync: false` per tutto ciò che è realmente segreto o non ancora noto: `DB_NAME`/`DB_USER`/`DB_PASSWORD`/`DB_HOST` (le credenziali Neon, esistono solo dopo aver creato il progetto Neon, mai scritte in questo file), `RESEND_API_KEY` (stessa chiave già in uso in `.env` locale), `FRONTEND_ACTIVATION_URL`/`FRONTEND_RESET_URL` (**segnaposto deliberato**: i deep link `legole://...` — validi per una build nativa, sezione 1 — non risolvono affatto in un browser; vanno sostituiti con l'URL https reale non appena il frontend ha un indirizzo web, altrimenti le email di attivazione/reset password inviate durante il test finirebbero su un link morto).
*   **Backend adattato per un host/DB esterni, senza toccare il comportamento locale/Docker esistente (`backend/settings.py`):**
    *   `ALLOWED_HOSTS = env.list('ALLOWED_HOSTS', default=['*'])` (era `['*']` fisso) — l'elenco è letto da env solo se impostato; `.env` locale e `.env.prod` Docker non lo impostano affatto, quindi restano al comportamento permissivo di sempre.
    *   `DATABASES['default']` ora include `CONN_HEALTH_CHECKS` (da `DB_CONN_HEALTH_CHECKS`, default `False`) e `OPTIONS: {'sslmode': ...}` (da `DB_SSLMODE`, presente solo se la variabile è impostata — altrimenti `OPTIONS` resta `{}` come prima, nessuna regressione). Neon richiede **entrambi**: `sslmode=require` perché accetta solo connessioni TLS (verificato nella loro documentazione ufficiale per Django), e `CONN_HEALTH_CHECKS=True` perché il compute Neon si sospende dopo un periodo di inattività sul piano free — senza il controllo di salute della connessione, Django rischierebbe di riusare una connessione che il server ha nel frattempo chiuso, causando un errore a caso sulla prima richiesta dopo una pausa.
*   **`le-gole-be/Dockerfile` — bind dinamico sulla porta:** `gunicorn ... --bind 0.0.0.0:${PORT:-8000}` (era `:8000` fisso) — Render assegna la porta di ascolto tramite la variabile d'ambiente `PORT`, decisa da Render stesso e non prevedibile in anticipo; il fallback a `8000` lascia invariato `docker-compose.yml` (dove `PORT` non è impostata).
*   **Driver DB invariato:** la documentazione Neon per Django consiglia `psycopg` (v3) al posto di `psycopg2`, ma non lo richiede — `psycopg2-binary` (già in uso, sezione 0) parla comunque il protocollo di rete standard di Postgres, e Neon è compatibile con qualunque client Postgres conforme. Cambiare driver non era necessario per far funzionare la connessione, solo la preferenza "moderna" suggerita da Neon per progetti nuovi; non ci sono benefici concreti per questo progetto che giustifichino il rischio di un cambio non richiesto.
*   **Ancora da fare prima che questo sia un deploy realmente completo:** creare il progetto Neon e incollare le credenziali reali nella dashboard Render (`sync: false` sopra), importare il Blueprint su Render, verificare il primo deploy end-to-end (migrazioni, healthcheck, un endpoint reale, e — punto delicato già noto, sezione 10 — che WeasyPrint generi ancora il PDF biglietto con le sole librerie di sistema del Dockerfile), poi sostituire `FRONTEND_ACTIVATION_URL`/`FRONTEND_RESET_URL` quando il frontend avrà un host. Nessuna di queste azioni può essere completata senza creare account su servizi esterni, quindi restano a carico dell'utente.
